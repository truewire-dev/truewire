//! Generated by truewire — do not edit by hand.

use std::sync::Arc;

use serde::{Deserialize, Serialize};
use truewire_core::paging::exhausted;
use truewire_core::{
    decode, dump, serde_json, CallOptions, HttpCall, HttpEndpoint, PaginatedResponse, Result,
    TimestampIso,
};

use crate::meta::DefaultMeta;
use crate::types::{Label, SimpleUser};

/// Present when the row is a pull request.
#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct IssuePullRequest {
    /// API URL of the pull request.
    pub url: String,
    /// Web page of the pull request.
    pub html_url: String,
    /// When the pull request was merged; null if not merged.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[serde(with = "truewire_core::validation::double_option")]
    pub merged_at: Option<Option<TimestampIso>>,
    /// Keys the spec does not document, kept as they came.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum RequestState {
    #[serde(rename = "open")]
    Open,
    #[serde(rename = "closed")]
    Closed,
    #[serde(rename = "all")]
    All,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum RequestSort {
    #[serde(rename = "created")]
    Created,
    #[serde(rename = "updated")]
    Updated,
    #[serde(rename = "comments")]
    Comments,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum RequestDirection {
    #[serde(rename = "asc")]
    Asc,
    #[serde(rename = "desc")]
    Desc,
}

/// Which repository, which issues, and which page.
#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct Request {
    /// Account owner of the repository, case-insensitive.
    pub owner: String,
    /// Repository name without the `.git` extension, case-insensitive.
    pub repo: String,
    /// Issue state to include.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<RequestState>,
    /// Comma-separated label names; every one must match.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labels: Option<String>,
    /// Sort key.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sort: Option<RequestSort>,
    /// Sort direction.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub direction: Option<RequestDirection>,
    /// Only issues updated at or after this instant.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub since: Option<TimestampIso>,
    /// Results per page, at most 100.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub per_page: Option<i64>,
    /// Page number of the results to fetch, starting at 1.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
    /// Keys the spec does not document, kept as they came.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum IssueState {
    #[serde(rename = "open")]
    Open,
    #[serde(rename = "closed")]
    Closed,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum IssueStateReason {
    #[serde(rename = "completed")]
    Completed,
    #[serde(rename = "not_planned")]
    NotPlanned,
    #[serde(rename = "reopened")]
    Reopened,
    #[serde(rename = "duplicate")]
    Duplicate,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum IssueAuthorAssociation {
    #[serde(rename = "COLLABORATOR")]
    Collaborator,
    #[serde(rename = "CONTRIBUTOR")]
    Contributor,
    #[serde(rename = "FIRST_TIMER")]
    FirstTimer,
    #[serde(rename = "FIRST_TIME_CONTRIBUTOR")]
    FirstTimeContributor,
    #[serde(rename = "MANNEQUIN")]
    Mannequin,
    #[serde(rename = "MEMBER")]
    Member,
    #[serde(rename = "NONE")]
    None,
    #[serde(rename = "OWNER")]
    Owner,
}

/// An issue, or a pull request seen as one.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Issue {
    /// Issue id.
    pub id: i64,
    /// GraphQL node id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_id: Option<String>,
    /// Issue number within the repository.
    pub number: i64,
    /// Title.
    pub title: String,
    /// Open or closed.
    pub state: IssueState,
    /// Why the issue is in its state, when GitHub recorded one.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[serde(with = "truewire_core::validation::double_option")]
    pub state_reason: Option<Option<IssueStateReason>>,
    /// Whether the conversation is locked.
    pub locked: bool,
    /// Author, when the account still exists.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[serde(with = "truewire_core::validation::double_option")]
    pub user: Option<Option<SimpleUser>>,
    /// Labels on the issue.
    pub labels: Vec<Label>,
    /// Number of comments.
    pub comments: i64,
    /// When the issue was opened.
    pub created_at: TimestampIso,
    /// When the issue last changed.
    pub updated_at: TimestampIso,
    /// When the issue was closed; null while open.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[serde(with = "truewire_core::validation::double_option")]
    pub closed_at: Option<Option<TimestampIso>>,
    /// Body in Markdown, when set.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[serde(with = "truewire_core::validation::double_option")]
    pub body: Option<Option<String>>,
    /// Web page of the issue.
    pub html_url: String,
    /// How the author relates to the repository.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub author_association: Option<IssueAuthorAssociation>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pull_request: Option<IssuePullRequest>,
    /// Keys the spec does not document, kept as they came.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

pub type Issues = Vec<Issue>;

/// `list_paged`'s request: `Request` without `page`, which the walk advances.
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ListPagedRequest {
    /// Account owner of the repository, case-insensitive.
    pub owner: String,
    /// Repository name without the `.git` extension, case-insensitive.
    pub repo: String,
    /// Issue state to include.
    pub state: Option<RequestState>,
    /// Comma-separated label names; every one must match.
    pub labels: Option<String>,
    /// Sort key.
    pub sort: Option<RequestSort>,
    /// Sort direction.
    pub direction: Option<RequestDirection>,
    /// Only issues updated at or after this instant.
    pub since: Option<TimestampIso>,
    /// Results per page, at most 100.
    pub per_page: Option<i64>,
    /// Keys the spec does not document, sent as they are.
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl ListPagedRequest {
    /// The `Request` for one page of the walk.
    fn at(&self, page: Option<i64>) -> Request {
        Request {
            owner: self.owner.clone(),
            repo: self.repo.clone(),
            state: self.state,
            labels: self.labels.clone(),
            sort: self.sort,
            direction: self.direction,
            since: self.since,
            per_page: self.per_page,
            page,
            extra: self.extra.clone(),
        }
    }
}

/// List issues in a repository. GitHub returns pull requests here too; a row with `pull_request` set is one.
#[derive(Clone)]
pub struct List {
    core: Arc<dyn HttpEndpoint<DefaultMeta>>,
}

impl List {
    pub fn new(core: Arc<dyn HttpEndpoint<DefaultMeta>>) -> Self {
        Self { core }
    }

    /// List issues in a repository. GitHub returns pull requests here too; a row with `pull_request` set is one.
    ///
    /// Paged variant of `list`: await it for every row, or walk `rows()`/`pages()` one page at a time.
    ///
    /// See <https://docs.github.com/en/rest/issues/issues#list-repository-issues>.
    pub fn list_paged(
        &self,
        request: ListPagedRequest,
        options: CallOptions,
    ) -> PaginatedResponse<Issue, i64> {
        let endpoint = self.clone();
        let size = request.per_page.unwrap_or(30);
        let size = Some(size as usize);
        let next = move |page: i64| {
            let endpoint = endpoint.clone();
            let request = request.clone();
            let options = options.clone();
            async move {
                let request = request.at(Some(page));
                let response = endpoint.list(request, options).await?;
                let rows = response;
                if exhausted(rows.len(), size) {
                    return Ok((rows, None));
                }
                Ok((rows, Some(page + 1)))
            }
        };
        PaginatedResponse::new(1, next)
    }

    /// List issues in a repository. GitHub returns pull requests here too; a row with `pull_request` set is one.
    ///
    /// See <https://docs.github.com/en/rest/issues/issues#list-repository-issues>.
    pub async fn list(&self, request: Request, options: CallOptions) -> Result<Issues> {
        let raw = self.list_raw(request, options).await?;
        decode(raw)
    }

    /// `list` without validation: the wire body as it came.
    pub async fn list_raw(
        &self,
        request: Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        let meta = DefaultMeta { public: Some(true) };
        let call = HttpCall {
            method: Some("GET"),
            path: "/repos/{owner}/{repo}/issues",
            request: Some(dump(&request)?),
            meta: &meta,
            options,
        };
        self.core.request(call).await
    }
}
