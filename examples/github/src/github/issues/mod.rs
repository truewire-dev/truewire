//! Generated by truewire — do not edit by hand.

pub mod list;

use std::sync::Arc;

use truewire_core::{serde_json, CallOptions, HttpEndpoint, PaginatedResponse, Result};

use crate::meta::DefaultMeta;

/// Issues and, through the same list, pull requests.
///
/// See <https://docs.github.com/en/rest/issues>.
#[derive(Clone)]
pub struct Issues {
    list: list::List,
}

impl Issues {
    pub fn new(core: Arc<dyn HttpEndpoint<DefaultMeta>>) -> Self {
        Self {
            list: list::List::new(core),
        }
    }

    /// List issues in a repository. GitHub returns pull requests here too; a row with `pull_request` set is one.
    ///
    /// Paged variant of `list`: await it for every row, or walk `rows()`/`pages()` one page at a time.
    ///
    /// See <https://docs.github.com/en/rest/issues/issues#list-repository-issues>.
    pub fn list_paged(
        &self,
        request: list::ListPagedRequest,
        options: CallOptions,
    ) -> PaginatedResponse<list::Issue, i64> {
        self.list.list_paged(request, options)
    }

    /// List issues in a repository. GitHub returns pull requests here too; a row with `pull_request` set is one.
    ///
    /// See <https://docs.github.com/en/rest/issues/issues#list-repository-issues>.
    pub async fn list(&self, request: list::Request, options: CallOptions) -> Result<list::Issues> {
        self.list.list(request, options).await
    }

    /// `list` without validation: the wire body as it came.
    pub async fn list_raw(
        &self,
        request: list::Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        self.list.list_raw(request, options).await
    }
}
