//! Generated by truewire — do not edit by hand.

pub mod get;
pub mod get_commit;
pub mod list_commits;
pub mod list_releases;
pub mod list_tags;

use std::sync::Arc;

use truewire_core::{serde_json, CallOptions, HttpEndpoint, PaginatedResponse, Result};

use crate::meta::DefaultMeta;
use crate::types::Commit;

/// Repositories: metadata, commits, tags and releases.
///
/// See <https://docs.github.com/en/rest/repos>.
#[derive(Clone)]
pub struct Repos {
    get: get::Get,
    get_commit: get_commit::GetCommit,
    list_commits: list_commits::ListCommits,
    list_releases: list_releases::ListReleases,
    list_tags: list_tags::ListTags,
}

impl Repos {
    pub fn new(core: Arc<dyn HttpEndpoint<DefaultMeta>>) -> Self {
        Self {
            get: get::Get::new(core.clone()),
            get_commit: get_commit::GetCommit::new(core.clone()),
            list_commits: list_commits::ListCommits::new(core.clone()),
            list_releases: list_releases::ListReleases::new(core.clone()),
            list_tags: list_tags::ListTags::new(core),
        }
    }

    /// Get a repository. Metadata, counts and the license as GitHub reports them.
    ///
    /// See <https://docs.github.com/en/rest/repos/repos#get-a-repository>.
    pub async fn get(
        &self,
        request: get::Request,
        options: CallOptions,
    ) -> Result<get::Repository> {
        self.get.get(request, options).await
    }

    /// `get` without validation: the wire body as it came.
    pub async fn get_raw(
        &self,
        request: get::Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        self.get.get_raw(request, options).await
    }

    /// Get one commit with its diff statistics and changed files.
    ///
    /// See <https://docs.github.com/en/rest/commits/commits#get-a-commit>.
    pub async fn get_commit(
        &self,
        request: get_commit::Request,
        options: CallOptions,
    ) -> Result<get_commit::CommitDetail> {
        self.get_commit.get_commit(request, options).await
    }

    /// `get_commit` without validation: the wire body as it came.
    pub async fn get_commit_raw(
        &self,
        request: get_commit::Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        self.get_commit.get_commit_raw(request, options).await
    }

    /// List commits reachable from a branch or sha, newest first. Pages are joined by `page`/`per_page`; the generated `list_commits_paged` walks them until a short page.
    ///
    /// Paged variant of `list_commits`: await it for every row, or walk `rows()`/`pages()` one page at a time.
    ///
    /// See <https://docs.github.com/en/rest/commits/commits#list-commits>.
    pub fn list_commits_paged(
        &self,
        request: list_commits::ListCommitsPagedRequest,
        options: CallOptions,
    ) -> PaginatedResponse<Commit, i64> {
        self.list_commits.list_commits_paged(request, options)
    }

    /// List commits reachable from a branch or sha, newest first. Pages are joined by `page`/`per_page`; the generated `list_commits_paged` walks them until a short page.
    ///
    /// See <https://docs.github.com/en/rest/commits/commits#list-commits>.
    pub async fn list_commits(
        &self,
        request: list_commits::Request,
        options: CallOptions,
    ) -> Result<list_commits::Commits> {
        self.list_commits.list_commits(request, options).await
    }

    /// `list_commits` without validation: the wire body as it came.
    pub async fn list_commits_raw(
        &self,
        request: list_commits::Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        self.list_commits.list_commits_raw(request, options).await
    }

    /// List releases, newest first. Includes drafts and pre-releases the caller can see.
    ///
    /// Paged variant of `list_releases`: await it for every row, or walk `rows()`/`pages()` one page at a time.
    ///
    /// See <https://docs.github.com/en/rest/releases/releases#list-releases>.
    pub fn list_releases_paged(
        &self,
        request: list_releases::ListReleasesPagedRequest,
        options: CallOptions,
    ) -> PaginatedResponse<list_releases::Release, i64> {
        self.list_releases.list_releases_paged(request, options)
    }

    /// List releases, newest first. Includes drafts and pre-releases the caller can see.
    ///
    /// See <https://docs.github.com/en/rest/releases/releases#list-releases>.
    pub async fn list_releases(
        &self,
        request: list_releases::Request,
        options: CallOptions,
    ) -> Result<list_releases::Releases> {
        self.list_releases.list_releases(request, options).await
    }

    /// `list_releases` without validation: the wire body as it came.
    pub async fn list_releases_raw(
        &self,
        request: list_releases::Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        self.list_releases.list_releases_raw(request, options).await
    }

    /// List repository tags, newest first.
    ///
    /// Paged variant of `list_tags`: await it for every row, or walk `rows()`/`pages()` one page at a time.
    ///
    /// See <https://docs.github.com/en/rest/repos/repos#list-repository-tags>.
    pub fn list_tags_paged(
        &self,
        request: list_tags::ListTagsPagedRequest,
        options: CallOptions,
    ) -> PaginatedResponse<list_tags::Tag, i64> {
        self.list_tags.list_tags_paged(request, options)
    }

    /// List repository tags, newest first.
    ///
    /// See <https://docs.github.com/en/rest/repos/repos#list-repository-tags>.
    pub async fn list_tags(
        &self,
        request: list_tags::Request,
        options: CallOptions,
    ) -> Result<list_tags::Tags> {
        self.list_tags.list_tags(request, options).await
    }

    /// `list_tags` without validation: the wire body as it came.
    pub async fn list_tags_raw(
        &self,
        request: list_tags::Request,
        options: CallOptions,
    ) -> Result<serde_json::Value> {
        self.list_tags.list_tags_raw(request, options).await
    }
}
