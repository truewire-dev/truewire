{
  "docs": "https://docs.kraken.com/api-reference/account-data/request-export-report",
  "meta": {
    "signed": true
  },
  "redacted": [
    "nonce"
  ],
  "spec": {
    "kind": "rpc",
    "transports": [
      "http"
    ],
    "path": "/0/private/AddExport",
    "method": "POST",
    "description": "Request an export of trades or ledgers. The export is asynchronous: this call returns a report ID, not the data itself -- poll `ExportStatus` and retrieve the finished file with `RetrieveExport`.\n\n**API Key Permissions Required:** `Data - Export data`",
    "request": {
      "title": "AddExportRequest",
      "type": "object",
      "required": [
        "report",
        "description"
      ],
      "properties": {
        "report": {
          "description": "Type of data to export.",
          "type": "string",
          "enum": [
            "trades",
            "ledgers"
          ]
        },
        "format": {
          "description": "File format to export.",
          "type": "string",
          "enum": [
            "CSV",
            "TSV"
          ],
          "default": "CSV"
        },
        "description": {
          "description": "Description for the export.",
          "type": "string"
        },
        "fields": {
          "description": "Comma-delimited list of fields to include. Defaults to `all`. For `report: trades`: `ordertxid`, `time`, `ordertype`, `price`, `cost`, `fee`, `vol`, `margin`, `misc`, `ledgers`. For `report: ledgers`: `refid`, `time`, `type`, `subtype`, `aclass`, `asset`, `amount`, `fee`, `balance`, `wallet`.",
          "type": "string",
          "default": "all"
        },
        "starttm": {
          "description": "UNIX timestamp for report start time (defaults to the 1st of the current month).",
          "type": "integer"
        },
        "endtm": {
          "description": "UNIX timestamp for report end time (defaults to now).",
          "type": "integer"
        }
      }
    },
    "response": {
      "title": "AddExportResultFrame",
      "type": "object",
      "description": "Wire frame; the generated method returns `result`.",
      "required": [
        "error",
        "result"
      ],
      "properties": {
        "error": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Wire envelope field; see the core."
        },
        "result": {
          "title": "AddExportResult",
          "description": "The newly requested export report.",
          "type": "object",
          "properties": {
            "id": {
              "description": "Report ID, for use with `ExportStatus`/`RetrieveExport`/`RemoveExport`.",
              "type": "string"
            }
          }
        }
      }
    }
  },
  "envelope": {
    "payload": "result"
  }
}
