# Generated by truewire — do not edit by hand.
from typing_extensions import Any, Literal, NotRequired, TypedDict, overload
from kraken.core.endpoint.rpc import RpcEndpoint


class AccountTransferResult(TypedDict):
  """Outcome of the account transfer."""

  transfer_id: NotRequired[str]
  """Unique identifier for the transfer."""
  status: NotRequired[Literal['pending', 'complete']]
  """Transfer status."""


RequestKeywords = TypedDict('RequestKeywords', {'from': str})
"""
- `from`: Public account ID of the source account (e.g. `ABCD 1234 EFGH 5678`).
"""


class Request(RequestKeywords):
  asset: str
  """Asset being transferred (e.g. `XBT`)."""
  asset_class: NotRequired[Literal['currency', 'tokenized_asset']]
  """Asset class of the asset being transferred."""
  amount: str
  """Amount of the asset to transfer."""
  to: str
  """Public account ID of the destination account (e.g. `IJKL 0987 MNOP 6543`)."""


class AccountTransfer(RpcEndpoint):
  """Transfer funds to and from the master account and its subaccounts. Must be called using an API key from the master account. Subaccounts are currently only available to institutional clients.

  **API Key Permissions Required:** `Funds permissions - Withdraw`"""

  @overload
  async def account_transfer(
    self,
    *,
    asset: str,
    asset_class: Literal['currency', 'tokenized_asset'] | None = None,
    amount: str,
    from_: str,
    to: str,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def account_transfer(
    self,
    *,
    asset: str,
    asset_class: Literal['currency', 'tokenized_asset'] | None = None,
    amount: str,
    from_: str,
    to: str,
    validate: bool | None = None,
  ) -> AccountTransferResult: ...
  async def account_transfer(
    self,
    *,
    asset: str,
    asset_class: Literal['currency', 'tokenized_asset'] | None = None,
    amount: str,
    from_: str,
    to: str,
    validate: bool | None = None,
  ) -> AccountTransferResult:
    """Transfer funds to and from the master account and its subaccounts. Must be called using an API key from the master account. Subaccounts are currently only available to institutional clients.

    **API Key Permissions Required:** `Funds permissions - Withdraw`

    Args:
      asset: Asset being transferred (e.g. `XBT`).
      asset_class: Asset class of the asset being transferred.
      amount: Amount of the asset to transfer.
      from_: Public account ID of the source account (e.g. `ABCD 1234 EFGH 5678`).
      to: Public account ID of the destination account (e.g. `IJKL 0987 MNOP 6543`).
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/subaccounts/account-transfer)
    """
    request: Request = {'asset': asset, 'amount': amount, 'from': from_, 'to': to}
    if asset_class is not None:
      request['asset_class'] = asset_class
    return await self.request(
      request,
      method='POST',
      path='/0/private/AccountTransfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=AccountTransferResult,
    )
