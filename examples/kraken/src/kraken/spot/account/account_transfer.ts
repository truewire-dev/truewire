// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** Outcome of the account transfer. */
export interface AccountTransferResult {
  /** Unique identifier for the transfer. */
  transfer_id?: string
  /** Transfer status. */
  status?: 'pending' | 'complete'
}

export const AccountTransferResult: Codec<AccountTransferResult> = t.object({
  transfer_id: t.optional(t.string),
  status: t.optional(t.literal('pending', 'complete')),
})

export interface Request {
  /** Asset being transferred (e.g. `XBT`). */
  asset: string
  /** Asset class of the asset being transferred. */
  asset_class?: 'currency' | 'tokenized_asset'
  /** Amount of the asset to transfer. */
  amount: string
  /** Public account ID of the source account (e.g. `ABCD 1234 EFGH 5678`). */
  from: string
  /** Public account ID of the destination account (e.g. `IJKL 0987 MNOP 6543`). */
  to: string
}

export const Request: Codec<Request> = t.object({
  asset: t.string,
  asset_class: t.optional(t.literal('currency', 'tokenized_asset')),
  amount: t.string,
  from: t.string,
  to: t.string,
})

/**
 * Transfer funds to and from the master account and its subaccounts. Must be called using an API key from the master account. Subaccounts are currently only available to institutional clients.
 *
 * **API Key Permissions Required:** `Funds permissions - Withdraw`
 */
export class AccountTransfer {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  accountTransfer(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Transfer funds to and from the master account and its subaccounts. Must be called using an API key from the master account. Subaccounts are currently only available to institutional clients.
   *
   * **API Key Permissions Required:** `Funds permissions - Withdraw`
   *
   * @see https://docs.kraken.com/api-reference/subaccounts/account-transfer
   */
  accountTransfer(request: Request, options?: CallOptions): Promise<AccountTransferResult>
  async accountTransfer(request: Request, options?: CallOptions): Promise<AccountTransferResult> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/AccountTransfer',
      request,
      requestCodec: Request,
      responseCodec: AccountTransferResult,
      meta: { signed: true },
      ...options,
    })
  }
}
