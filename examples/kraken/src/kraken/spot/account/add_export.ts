// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** The newly requested export report. */
export interface AddExportResult {
  /** Report ID, for use with `ExportStatus`/`RetrieveExport`/`RemoveExport`. */
  id?: string
}

export const AddExportResult: Codec<AddExportResult> = t.object({
  id: t.optional(t.string),
})

export interface Request {
  /** Type of data to export. */
  report: 'trades' | 'ledgers'
  /** File format to export. */
  format?: 'CSV' | 'TSV'
  /** Description for the export. */
  description: string
  /** Comma-delimited list of fields to include. Defaults to `all`. For `report: trades`: `ordertxid`, `time`, `ordertype`, `price`, `cost`, `fee`, `vol`, `margin`, `misc`, `ledgers`. For `report: ledgers`: `refid`, `time`, `type`, `subtype`, `aclass`, `asset`, `amount`, `fee`, `balance`, `wallet`. */
  fields?: string
  /** UNIX timestamp for report start time (defaults to the 1st of the current month). */
  starttm?: number
  /** UNIX timestamp for report end time (defaults to now). */
  endtm?: number
}

export const Request: Codec<Request> = t.object({
  report: t.literal('trades', 'ledgers'),
  format: t.optional(t.literal('CSV', 'TSV')),
  description: t.string,
  fields: t.optional(t.string),
  starttm: t.optional(t.integer),
  endtm: t.optional(t.integer),
})

/**
 * Request an export of trades or ledgers. The export is asynchronous: this call returns a report ID, not the data itself -- poll `ExportStatus` and retrieve the finished file with `RetrieveExport`.
 *
 * **API Key Permissions Required:** `Data - Export data`
 */
export class AddExport {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  addExport(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Request an export of trades or ledgers. The export is asynchronous: this call returns a report ID, not the data itself -- poll `ExportStatus` and retrieve the finished file with `RetrieveExport`.
   *
   * **API Key Permissions Required:** `Data - Export data`
   *
   * @see https://docs.kraken.com/api-reference/account-data/request-export-report
   */
  addExport(request: Request, options?: CallOptions): Promise<AddExportResult>
  async addExport(request: Request, options?: CallOptions): Promise<AddExportResult> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/AddExport',
      request,
      requestCodec: Request,
      responseCodec: AddExportResult,
      meta: { signed: true },
      ...options,
    })
  }
}
