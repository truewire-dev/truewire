// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type Decimal, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** Extended balance detail for one asset. */
export interface ExtendedBalance {
  /** Total balance amount for this asset. */
  balance?: Decimal
  /** Total credit amount. Only present if the account has a credit line. */
  credit?: string
  /** Used credit amount. Only present if the account has a credit line. */
  credit_used?: string
  /** Total held amount for this asset (spot non-margin orders only). */
  hold_trade?: Decimal
}

export const ExtendedBalance: Codec<ExtendedBalance> = t.object({
  balance: t.optional(t.decimal),
  credit: t.optional(t.string),
  credit_used: t.optional(t.string),
  hold_trade: t.optional(t.decimal),
})

export interface Request {
  /** Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity, `base` displays in terms of SPV tokens. */
  rebase_multiplier?: 'rebased' | 'base' | null
}

export const Request: Codec<Request> = t.object({
  rebase_multiplier: t.optional(t.nullable(t.literal('rebased', 'base'))),
})

export type ExtendedBalances = Record<string, ExtendedBalance>

export const ExtendedBalances: Codec<ExtendedBalances> = t.record(ExtendedBalance)

/**
 * Retrieve all extended account balances, including credit and held amounts. Balance available for trading is `balance + credit - credit_used - hold_trade`. Held amounts only include spot non-margin orders.
 *
 * **Note on Staking/Earn assets:** assets migrated from the legacy Staking system to the new Earn system may appear with symbol extensions -- `.B`, `.F`, `.S`, `.M`, `.T` -- these are read-only for transacting; use the base asset instead.
 *
 * **API Key Permissions Required:** `Funds permissions - Query`
 */
export class BalanceEx {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  balanceEx(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Retrieve all extended account balances, including credit and held amounts. Balance available for trading is `balance + credit - credit_used - hold_trade`. Held amounts only include spot non-margin orders.
   *
   * **Note on Staking/Earn assets:** assets migrated from the legacy Staking system to the new Earn system may appear with symbol extensions -- `.B`, `.F`, `.S`, `.M`, `.T` -- these are read-only for transacting; use the base asset instead.
   *
   * **API Key Permissions Required:** `Funds permissions - Query`
   *
   * @see https://docs.kraken.com/api-reference/account-data/get-extended-balance
   */
  balanceEx(request?: Request, options?: CallOptions): Promise<ExtendedBalances>
  async balanceEx(request?: Request, options?: CallOptions): Promise<ExtendedBalances> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/BalanceEx',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: ExtendedBalances,
      meta: { signed: true },
      ...options,
    })
  }
}
