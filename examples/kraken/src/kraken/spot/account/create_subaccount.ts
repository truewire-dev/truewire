// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Username for the subaccount. */
  username: string
  /** Email address for the subaccount. */
  email: string
}

export const Request: Codec<Request> = t.object({
  username: t.string,
  email: t.string,
})

export type Response = boolean

export const Response: Codec<Response> = t.boolean

/**
 * Create a trading subaccount. Must be called using an API key from the master account. Subaccounts are currently only available to institutional clients; contact your Account Manager for details.
 *
 * **API Key Permissions Required:** `Funds permissions - Withdraw`
 */
export class CreateSubaccount {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  createSubaccount(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Create a trading subaccount. Must be called using an API key from the master account. Subaccounts are currently only available to institutional clients; contact your Account Manager for details.
   *
   * **API Key Permissions Required:** `Funds permissions - Withdraw`
   *
   * @see https://docs.kraken.com/api-reference/subaccounts/create-subaccount
   */
  createSubaccount(request: Request, options?: CallOptions): Promise<Response>
  async createSubaccount(request: Request, options?: CallOptions): Promise<Response> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/CreateSubaccount',
      request,
      requestCodec: Request,
      responseCodec: Response,
      meta: { signed: true },
      ...options,
    })
  }
}
