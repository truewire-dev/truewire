// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** One open margin position. */
export interface OpenPosition {
  /** Order ID responsible for the position. */
  ordertxid?: string
  /** Asset class of the position. */
  class?: string
  /** Position status. */
  posstatus?: 'open'
  /** Asset pair. */
  pair?: string
  /** Unix timestamp of the trade. */
  time?: number
  /** Direction of the position. */
  type?: 'buy' | 'sell'
  /** Order type used to open the position. */
  ordertype?: 'market' | 'limit' | 'iceberg' | 'stop-loss' | 'take-profit' | 'stop-loss-limit' | 'take-profit-limit' | 'trailing-stop' | 'trailing-stop-limit' | 'settle-position'
  /** Opening cost of the position (quote currency). */
  cost?: string
  /** Opening fee of the position (quote currency). */
  fee?: string
  /** Position opening size (base currency). */
  vol?: string
  /** Quantity closed (base currency). */
  vol_closed?: string
  /** Initial margin consumed (quote currency). */
  margin?: string
  /** Current value of the remaining position. Only present if `docalcs` requested. */
  value?: string
  /** Unrealised P&L of the remaining position. Only present if `docalcs` requested. */
  net?: string
  /** Funding cost and term of the position (e.g. "0.0100% per 4 hours"). */
  terms?: string
  /** Timestamp of the next margin rollover fee. */
  rollovertm?: string
  /** Comma-delimited list of additional info. */
  misc?: string
  /** Comma-delimited list of opening order flags. */
  oflags?: string
}

export const OpenPosition: Codec<OpenPosition> = t.object({
  ordertxid: t.optional(t.string),
  class: t.optional(t.string),
  posstatus: t.optional(t.literal('open')),
  pair: t.optional(t.string),
  time: t.optional(t.number),
  type: t.optional(t.literal('buy', 'sell')),
  ordertype: t.optional(t.literal('market', 'limit', 'iceberg', 'stop-loss', 'take-profit', 'stop-loss-limit', 'take-profit-limit', 'trailing-stop', 'trailing-stop-limit', 'settle-position')),
  cost: t.optional(t.string),
  fee: t.optional(t.string),
  vol: t.optional(t.string),
  vol_closed: t.optional(t.string),
  margin: t.optional(t.string),
  value: t.optional(t.string),
  net: t.optional(t.string),
  terms: t.optional(t.string),
  rollovertm: t.optional(t.string),
  misc: t.optional(t.string),
  oflags: t.optional(t.string),
})

export interface Request {
  /** Comma-delimited list of txids to limit output to. */
  txid?: string
  /** Whether to include P&L calculations. */
  docalcs?: boolean
  /** Consolidate positions by market/pair. */
  consolidation?: 'market'
  /** Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity, `base` displays in terms of SPV tokens. */
  rebase_multiplier?: 'rebased' | 'base' | null
}

export const Request: Codec<Request> = t.object({
  txid: t.optional(t.string),
  docalcs: t.optional(t.boolean),
  consolidation: t.optional(t.literal('market')),
  rebase_multiplier: t.optional(t.nullable(t.literal('rebased', 'base'))),
})

export type OpenPositionsResponse = Record<string, OpenPosition>

export const OpenPositionsResponse: Codec<OpenPositionsResponse> = t.record(OpenPosition)

/**
 * Get information about open margin positions.
 *
 * **API Key Permissions Required:** `Orders and trades - Query open orders & trades`
 */
export class OpenPositions {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  openPositions(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Get information about open margin positions.
   *
   * **API Key Permissions Required:** `Orders and trades - Query open orders & trades`
   *
   * @see https://docs.kraken.com/api-reference/account-data/get-open-positions
   */
  openPositions(request?: Request, options?: CallOptions): Promise<OpenPositionsResponse>
  async openPositions(request?: Request, options?: CallOptions): Promise<OpenPositionsResponse> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/OpenPositions',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: OpenPositionsResponse,
      meta: { signed: true },
      ...options,
    })
  }
}
