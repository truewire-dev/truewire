// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, type TimestampNanos, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** One amend transaction. */
export interface OrderAmend {
  /** Kraken amend identifier. */
  amend_id?: string
  /** The type of amend transaction: `original` (original order values on order entry), `user` (user-requested amendment), or `restated` (engine order maintenance amendment). */
  amend_type?: 'original' | 'user' | 'restated'
  /** Order quantity in terms of the base asset. */
  order_qty?: string
  /** The quantity shown in the book for iceberg orders. */
  display_qty?: string
  /** Remaining un-traded quantity on the order. */
  remaining_qty?: string
  /** The limit price restriction on the order. */
  limit_price?: string
  /** The trigger price on trigger order types. */
  trigger_price?: string
  /** Description of the reason for this amend. */
  reason?: string
  /** Indicates if the transaction was restricted from taking liquidity. */
  post_only?: boolean
  /** The UNIX timestamp for the amend transaction, in nanosecond precision. */
  timestamp?: TimestampNanos
}

export const OrderAmend: Codec<OrderAmend> = t.object({
  amend_id: t.optional(t.string),
  amend_type: t.optional(t.literal('original', 'user', 'restated')),
  order_qty: t.optional(t.string),
  display_qty: t.optional(t.string),
  remaining_qty: t.optional(t.string),
  limit_price: t.optional(t.string),
  trigger_price: t.optional(t.string),
  reason: t.optional(t.string),
  post_only: t.optional(t.boolean),
  timestamp: t.optional(t.epochNanos),
})

export interface Request {
  /** The Kraken order identifier for the amended order. */
  order_id: string
  /** Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity, `base` displays in terms of SPV tokens. */
  rebase_multiplier?: 'rebased' | 'base' | null
}

export const Request: Codec<Request> = t.object({
  order_id: t.string,
  rebase_multiplier: t.optional(t.nullable(t.literal('rebased', 'base'))),
})

/** The amend transaction history for one order. */
export interface OrderAmends {
  /** Total count of new and amend transactions (including the original order). */
  count?: number
  /** Amend transactions, ordered by ascending amend timestamp. */
  amends?: OrderAmend[]
}

export const OrderAmends: Codec<OrderAmends> = t.object({
  count: t.optional(t.integer),
  amends: t.optional(t.array(OrderAmend)),
})

/**
 * Retrieve an audit trail of amend transactions on the specified order. The list is ordered by ascending amend timestamp; the first entry contains the original order parameters and has `amend_type` of `original`.
 *
 * **API Key Permissions Required:** `Orders and trades - Query open orders & trades` or `Orders and trades - Query closed orders & trades`, depending on the status of the order.
 */
export class OrderAmendsEndpoint {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  orderAmends(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Retrieve an audit trail of amend transactions on the specified order. The list is ordered by ascending amend timestamp; the first entry contains the original order parameters and has `amend_type` of `original`.
   *
   * **API Key Permissions Required:** `Orders and trades - Query open orders & trades` or `Orders and trades - Query closed orders & trades`, depending on the status of the order.
   *
   * @see https://docs.kraken.com/api-reference/account-data/get-order-amends
   */
  orderAmends(request: Request, options?: CallOptions): Promise<OrderAmends>
  async orderAmends(request: Request, options?: CallOptions): Promise<OrderAmends> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/OrderAmends',
      request,
      requestCodec: Request,
      responseCodec: OrderAmends,
      meta: { signed: true },
      ...options,
    })
  }
}
