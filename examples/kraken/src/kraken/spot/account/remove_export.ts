// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** Outcome of the delete/cancel operation. Exactly one of `delete`/`cancel` is present, matching the requested `type`. */
export interface RemoveExportResult {
  /** Whether deletion succeeded. Present when `type: "delete"` was requested. */
  delete?: boolean
  /** Whether cancellation succeeded. Present when `type: "cancel"` was requested. */
  cancel?: boolean
}

export const RemoveExportResult: Codec<RemoveExportResult> = t.object({
  delete: t.optional(t.boolean),
  cancel: t.optional(t.boolean),
})

export interface Request {
  /** ID of the report to delete or cancel. */
  id: string
  /** `delete` can only be used for reports that have already been processed. Use `cancel` for queued or processing reports. */
  type: 'cancel' | 'delete'
}

export const Request: Codec<Request> = t.object({
  id: t.string,
  type: t.literal('cancel', 'delete'),
})

/**
 * Delete an exported trades/ledgers report. `delete` can only be used for reports that have already been processed; use `cancel` for reports that are still queued or processing.
 *
 * **API Key Permissions Required:** `Data - Export data`
 */
export class RemoveExport {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  removeExport(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Delete an exported trades/ledgers report. `delete` can only be used for reports that have already been processed; use `cancel` for reports that are still queued or processing.
   *
   * **API Key Permissions Required:** `Data - Export data`
   *
   * @see https://docs.kraken.com/api-reference/account-data/delete-export-report
   */
  removeExport(request: Request, options?: CallOptions): Promise<RemoveExportResult>
  async removeExport(request: Request, options?: CallOptions): Promise<RemoveExportResult> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/RemoveExport',
      request,
      requestCodec: Request,
      responseCodec: RemoveExportResult,
      meta: { signed: true },
      ...options,
    })
  }
}
