// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Report ID to retrieve. */
  id: string
}

export const Request: Codec<Request> = t.object({
  id: t.string,
})

/** Metadata about the binary zip archive body, captured in place of the raw bytes (see `notes`). The real, hand-written `retrieve_export` method returns `bytes`, not this shape -- this schema documents the spec/example convention only. */
export interface RetrieveExportResult {
  /** The real response's `Content-Type` header. Observed live as `application/zip`, not the `application/octet-stream` upstream's OpenAPI declares -- see `notes`. */
  content_type: string
  /** Size of the binary body in bytes. */
  content_length: number
  /** First 4 bytes of the body, hex-encoded, confirming the ZIP local-file-header signature (`504b0304` = `PK\x03\x04`). */
  magic_bytes: string
  /** Free-text explanation of why this is metadata rather than the raw archive. */
  note?: string
}

export const RetrieveExportResult: Codec<RetrieveExportResult> = t.object({
  content_type: t.string,
  content_length: t.integer,
  magic_bytes: t.string,
  note: t.optional(t.string),
})

/**
 * Retrieve a processed data export. Unlike every other Account Data endpoint, the response is not the standard `{error, result}` JSON envelope -- it is the raw export file itself.
 *
 * **API Key Permissions Required:** `Data - Export data`
 */
export class RetrieveExport {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  retrieveExport(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Retrieve a processed data export. Unlike every other Account Data endpoint, the response is not the standard `{error, result}` JSON envelope -- it is the raw export file itself.
   *
   * **API Key Permissions Required:** `Data - Export data`
   *
   * @see https://docs.kraken.com/api-reference/account-data/retrieve-data-export
   */
  retrieveExport(request: Request, options?: CallOptions): Promise<RetrieveExportResult>
  async retrieveExport(request: Request, options?: CallOptions): Promise<RetrieveExportResult> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/RetrieveExport',
      request,
      requestCodec: Request,
      responseCodec: RetrieveExportResult,
      meta: { signed: true },
      ...options,
    })
  }
}
