# Generated by truewire — do not edit by hand.
from decimal import Decimal
from truewire_core.exceptions import LogicError
from typing_extensions import (
  Any,
  AsyncIterator,
  Literal,
  NotRequired,
  TypedDict,
  overload,
)
from kraken.core.endpoint.rpc import RpcEndpoint


class HistoricalTrade(TypedDict):
  """One historical trade."""

  ordertxid: NotRequired[str]
  """Order responsible for execution of the trade."""
  postxid: NotRequired[str]
  """Position responsible for execution of the trade."""
  pair: NotRequired[str]
  """Asset pair."""
  time: float
  """Unix timestamp of the trade."""
  type: NotRequired[Literal['buy', 'sell']]
  """Type of order."""
  ordertype: NotRequired[
    Literal[
      'market',
      'limit',
      'iceberg',
      'stop-loss',
      'take-profit',
      'stop-loss-limit',
      'take-profit-limit',
      'trailing-stop',
      'trailing-stop-limit',
      'settle-position',
    ]
  ]
  """Order type."""
  price: NotRequired[Decimal]
  """Average price the order was executed at (quote currency)."""
  cost: NotRequired[Decimal]
  """Total cost of the order (quote currency)."""
  fee: NotRequired[Decimal]
  """Total fee (quote currency)."""
  vol: NotRequired[Decimal]
  """Volume (base currency)."""
  margin: NotRequired[Decimal]
  """Initial margin (quote currency)."""
  leverage: NotRequired[str]
  """Amount of leverage used in the trade."""
  misc: NotRequired[str]
  """Comma-delimited list of miscellaneous info: `closing` -- trade closes all or part of a position."""
  ledgers: NotRequired[list[str]]
  """List of ledger ids for entries associated with the trade (if requested)."""
  trade_id: NotRequired[int]
  """Unique identifier of the trade executed."""
  maker: NotRequired[bool]
  """`true` if the trade was executed with the user as the maker, `false` if taker."""
  aclass: NotRequired[str]
  """Asset class of the traded pair."""
  tradeordertype: NotRequired[str]
  """Order type of the trade (e.g. `market`, `limit`). May differ from `ordertype` when the order type was converted on execution."""
  posstatus: NotRequired[Literal['open', 'closed']]
  """Position status. Only present if the trade opened a position."""
  cprice: NotRequired[float]
  """Average price of the closed portion of the position (quote currency). Only present if the trade opened a position."""
  ccost: NotRequired[float]
  """Total cost of the closed portion of the position (quote currency). Only present if the trade opened a position."""
  cfee: NotRequired[float]
  """Total fee of the closed portion of the position (quote currency). Only present if the trade opened a position."""
  cvol: NotRequired[float]
  """Total volume of the closed portion of the position (quote currency). Only present if the trade opened a position."""
  cmargin: NotRequired[float]
  """Total margin freed in the closed portion of the position (quote currency). Only present if the trade opened a position."""
  net: NotRequired[float]
  """Net profit/loss of the closed portion of the position (quote currency). Only present if the trade opened a position."""
  trades: NotRequired[list[str]]
  """List of closing trades for the position (if available). Only present if the trade opened a position."""


class Request(TypedDict):
  type: NotRequired[
    Literal['all', 'any position', 'closed position', 'closing position', 'no position']
  ]
  """Type of trade."""
  trades: NotRequired[bool]
  """Whether or not to include trades related to position in output."""
  start: NotRequired[int]
  """Starting unix timestamp or trade tx ID of results (exclusive)."""
  end: NotRequired[int]
  """Ending unix timestamp or trade tx ID of results (inclusive)."""
  ofs: NotRequired[int]
  """Result offset for pagination."""
  without_count: NotRequired[bool]
  """If true, does not retrieve the count of matching trades. Request can be noticeably faster for users with many trades, as this avoids an extra database query."""
  consolidate_taker: NotRequired[bool]
  """Whether or not to consolidate trades by individual taker trades."""
  ledgers: NotRequired[bool]
  """Whether or not to include related ledger ids for a given trade. Setting this to true will slow request performance."""
  rebase_multiplier: NotRequired[Literal['rebased', 'base'] | None]
  """Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity, `base` displays in terms of SPV tokens."""
  aclass: NotRequired[
    Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
  ]
  """Asset class of the `pair` filter."""
  pair: NotRequired[str]
  """Filter results to a single trading pair. An unrecognized pair returns `EQuery:Unknown asset pair`."""
  limit: NotRequired[int]
  """Number of trades returned per page. Values above 100 get clamped to 100."""


class TradeHistory(TypedDict):
  """Trade/fill history."""

  count: NotRequired[int]
  """Amount of available trades matching criteria. Omitted from the response when `without_count` is `true`."""
  trades: NotRequired[dict[str, HistoricalTrade]]
  """Trades, keyed by transaction ID."""


class TradesHistory(RpcEndpoint):
  """Retrieve information about trades/fills. By default, the most recent trades are returned. Unless otherwise stated, costs, fees, prices, and volumes are specified with the precision for the asset pair (`pair_decimals` and `lot_decimals`), not the individual assets' precision (`decimals`).

  **API Key Permissions Required:** `Orders and trades - Query closed orders & trades`"""

  @overload
  def trades_history_paged(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: Literal[False],
  ) -> AsyncIterator[Any]: ...
  @overload
  def trades_history_paged(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: Literal[True] | None = None,
  ) -> AsyncIterator[TradeHistory]: ...
  @overload
  def trades_history_paged(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[TradeHistory]: ...
  async def trades_history_paged(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    max_pages: int | None = None,
    validate: bool | None = None,
  ) -> AsyncIterator[TradeHistory]:
    """Retrieve information about trades/fills. By default, the most recent trades are returned. Unless otherwise stated, costs, fees, prices, and volumes are specified with the precision for the asset pair (`pair_decimals` and `lot_decimals`), not the individual assets' precision (`decimals`).

    **API Key Permissions Required:** `Orders and trades - Query closed orders & trades`

    Paged variant of `trades_history`: Advances `ofs` by `(limit if limit is not None else 50)` and stops once it has covered the `count` items the response reports, or after `max_pages` pages when one is given.

    Raises `LogicError` if a page omits `count`, or reports a value that disagrees with an earlier page of the same walk -- retry the whole walk from the start rather than trust a spliced result.

    Args:
      type: Type of trade.
      trades: Whether or not to include trades related to position in output.
      start: Starting unix timestamp or trade tx ID of results (exclusive).
      end: Ending unix timestamp or trade tx ID of results (inclusive).
      without_count: If true, does not retrieve the count of matching trades. Request can be noticeably faster for users with many trades, as this avoids an extra database query.
      consolidate_taker: Whether or not to consolidate trades by individual taker trades.
      ledgers: Whether or not to include related ledger ids for a given trade. Setting this to true will slow request performance.
      rebase_multiplier: Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity, `base` displays in terms of SPV tokens.
      aclass: Asset class of the `pair` filter.
      pair: Filter results to a single trading pair. An unrecognized pair returns `EQuery:Unknown asset pair`.
      limit: Number of trades returned per page. Values above 100 get clamped to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.
      max_pages: Stop after this many pages, even if the walk is not done.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/account-data/get-trades-history)
    """
    ofs = 0
    total_seen = None
    pages = 0
    while True:
      response = await self.trades_history(
        type,
        trades=trades,
        start=start,
        end=end,
        without_count=without_count,
        consolidate_taker=consolidate_taker,
        ledgers=ledgers,
        rebase_multiplier=rebase_multiplier,
        aclass=aclass,
        pair=pair,
        limit=limit,
        ofs=ofs,
        validate=validate,
      )
      yield response
      pages += 1
      if max_pages is not None and pages >= max_pages:
        break
      total = response.get('count') if response is not None else None
      total = int(total) if total is not None else None
      if total is None or (total_seen is not None and total != total_seen):
        raise LogicError(
          f'`trades_history_paged` needs a `total` on every page. The API omitted it here, or reported a value ({total}) that disagrees with an earlier page of this same walk ({total_seen}); retry the whole walk from the start.'
        )
      total_seen = total
      if pages * (limit if limit is not None else 50) >= total:
        break
      ofs += limit if limit is not None else 50

  @overload
  async def trades_history(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    ofs: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def trades_history(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    ofs: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    validate: Literal[True] | None = None,
  ) -> TradeHistory: ...
  @overload
  async def trades_history(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    ofs: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> TradeHistory: ...
  async def trades_history(
    self,
    type: Literal[
      'all', 'any position', 'closed position', 'closing position', 'no position'
    ]
    | None = None,
    *,
    trades: bool | None = None,
    start: int | None = None,
    end: int | None = None,
    ofs: int | None = None,
    without_count: bool | None = None,
    consolidate_taker: bool | None = None,
    ledgers: bool | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    aclass: Literal[
      'forex', 'equity_pair', 'futures_contract', 'synthetic_pair', 'external_pair'
    ]
    | None = None,
    pair: str | None = None,
    limit: int | None = None,
    validate: bool | None = None,
  ) -> TradeHistory:
    """Retrieve information about trades/fills. By default, the most recent trades are returned. Unless otherwise stated, costs, fees, prices, and volumes are specified with the precision for the asset pair (`pair_decimals` and `lot_decimals`), not the individual assets' precision (`decimals`).

    **API Key Permissions Required:** `Orders and trades - Query closed orders & trades`

    Args:
      type: Type of trade.
      trades: Whether or not to include trades related to position in output.
      start: Starting unix timestamp or trade tx ID of results (exclusive).
      end: Ending unix timestamp or trade tx ID of results (inclusive).
      ofs: Result offset for pagination.
      without_count: If true, does not retrieve the count of matching trades. Request can be noticeably faster for users with many trades, as this avoids an extra database query.
      consolidate_taker: Whether or not to consolidate trades by individual taker trades.
      ledgers: Whether or not to include related ledger ids for a given trade. Setting this to true will slow request performance.
      rebase_multiplier: Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity, `base` displays in terms of SPV tokens.
      aclass: Asset class of the `pair` filter.
      pair: Filter results to a single trading pair. An unrecognized pair returns `EQuery:Unknown asset pair`.
      limit: Number of trades returned per page. Values above 100 get clamped to 100.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/account-data/get-trades-history)
    """
    request: Request = Request()
    if type is not None:
      request['type'] = type
    if trades is not None:
      request['trades'] = trades
    if start is not None:
      request['start'] = start
    if end is not None:
      request['end'] = end
    if ofs is not None:
      request['ofs'] = ofs
    if without_count is not None:
      request['without_count'] = without_count
    if consolidate_taker is not None:
      request['consolidate_taker'] = consolidate_taker
    if ledgers is not None:
      request['ledgers'] = ledgers
    if rebase_multiplier is not None:
      request['rebase_multiplier'] = rebase_multiplier
    if aclass is not None:
      request['aclass'] = aclass
    if pair is not None:
      request['pair'] = pair
    if limit is not None:
      request['limit'] = limit
    return await self.request(
      request,
      method='POST',
      path='/0/private/TradesHistory',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=TradeHistory,
    )
