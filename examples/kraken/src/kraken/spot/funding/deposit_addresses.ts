// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** One deposit address for a particular asset and method. */
export interface DepositAddress {
  /** Deposit address. */
  address: string
  /** Expiration time as a unix timestamp, or `0` if not expiring. */
  expiretm: string
  /** Whether or not the address has ever been used. */
  new?: boolean
  /** Destination tag for XRP deposit addresses, or memo for STX/XLM/EOS deposit addresses. */
  tag?: string
  /** Memo for some deposit addresses (seen alongside `tag` in Kraken's own documented example response, though this property is not declared on the upstream schema -- see this endpoint's `notes`). */
  memo?: string
}

export const DepositAddress: Codec<DepositAddress> = t.object({
  address: t.string,
  expiretm: t.string,
  new: t.optional(t.boolean),
  tag: t.optional(t.string),
  memo: t.optional(t.string),
})

export interface Request {
  /** Asset being deposited. */
  asset: string
  /** Asset class (optional). Only `currency` is broadly used today; `tokenized_asset` covers xstocks-style tokenized equities. */
  aclass?: 'currency' | 'tokenized_asset'
  /** Name of the deposit method. */
  method: string
  /** Whether or not to generate a new address. */
  new?: boolean
  /** Amount you wish to deposit (only required for `method=Bitcoin Lightning`). */
  amount?: string | number
}

export const Request: Codec<Request> = t.object({
  asset: t.string,
  aclass: t.optional(t.literal('currency', 'tokenized_asset')),
  method: t.string,
  new: t.optional(t.boolean),
  amount: t.optional(t.union(t.string, t.integer, t.number)),
})

export type Response = DepositAddress[]

export const Response: Codec<Response> = t.array(DepositAddress)

/** Retrieve (or generate a new) deposit address for a particular asset and method. Requires the `Funds permissions - Query` API key permission. */
export class DepositAddresses {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  depositAddresses(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Retrieve (or generate a new) deposit address for a particular asset and method. Requires the `Funds permissions - Query` API key permission.
   *
   * @see https://docs.kraken.com/api-reference/funding/get-deposit-addresses
   */
  depositAddresses(request: Request, options?: CallOptions): Promise<Response>
  async depositAddresses(request: Request, options?: CallOptions): Promise<Response> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/DepositAddresses',
      request,
      requestCodec: Request,
      responseCodec: Response,
      meta: { signed: true },
      ...options,
    })
  }
}
