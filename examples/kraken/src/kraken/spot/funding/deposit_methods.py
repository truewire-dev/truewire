# Generated by truewire — do not edit by hand.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from kraken.core.endpoint.rpc import RpcEndpoint


DepositMethodKeywords = TypedDict(
  'DepositMethodKeywords',
  {
    'fee-percentage': NotRequired[Decimal],
    'address-setup-fee': NotRequired[str],
    'gen-address': NotRequired[bool],
  },
)
"""
  - `fee-percentage`: Percentage fee that will be paid (used instead of `fee` for some methods, e.g. Lightning).
  - `address-setup-fee`: Whether or not the method has an address setup fee.
  - `gen-address`: Whether new addresses can be generated for this method.
"""


class DepositMethod(DepositMethodKeywords):
  """One method available for depositing a particular asset."""

  method: str
  """Name of the deposit method, e.g. `Bitcoin`."""
  limit: str | Literal[False]
  """Maximum net amount that can be deposited right now, or `false` if there is no limit."""
  fee: Decimal
  """Amount of fees that will be paid (flat fee)."""
  minimum: NotRequired[Decimal]
  """Minimum net amount that can be deposited right now."""


class Request(TypedDict):
  asset: str
  """Asset being deposited, e.g. `XBT`."""
  aclass: NotRequired[Literal['currency', 'tokenized_asset']]
  """Asset class (optional). Only `currency` is broadly used today; `tokenized_asset` covers xstocks-style tokenized equities."""
  rebase_multiplier: NotRequired[Literal['rebased', 'base'] | None]
  """Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens."""


Response = list[DepositMethod]


class DepositMethods(RpcEndpoint):
  """Retrieve methods available for depositing a particular asset. Requires the `Funds permissions - Query` and `Funds permissions - Deposit` API key permissions."""

  async def deposit_methods(
    self,
    asset: str,
    *,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Retrieve methods available for depositing a particular asset. Requires the `Funds permissions - Query` and `Funds permissions - Deposit` API key permissions.

    Args:
      asset: Asset being deposited, e.g. `XBT`.
      aclass: Asset class (optional). Only `currency` is broadly used today; `tokenized_asset` covers xstocks-style tokenized equities.
      rebase_multiplier: Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/funding/get-deposit-methods)
    """
    request: Request = Request(asset=asset)
    if aclass is not None:
      request['aclass'] = aclass
    if rebase_multiplier is not None:
      request['rebase_multiplier'] = rebase_multiplier
    return await self.request(
      request,
      method='POST',
      path='/0/private/DepositMethods',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
