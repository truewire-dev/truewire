# Generated by truewire — do not edit by hand.
from truewire_core.types import TimestampSeconds
from typing_extensions import Any, Literal, NotRequired, TypedDict, overload
from kraken.core.endpoint.rpc import RpcEndpoint


Deposit0ItemKeywords = TypedDict(
  'Deposit0ItemKeywords', {'status-prop': NotRequired[Literal['return', 'onhold']]}
)
"""
- `status-prop`: Additional status property, if available: `return` for a return transaction initiated by Kraken, `onhold` when the deposit is on hold pending review.
"""


class Deposit0Item(Deposit0ItemKeywords):
  """One deposit record."""

  method: str
  """Name of deposit method."""
  aclass: str
  """Asset class."""
  asset: str
  """Asset."""
  refid: str
  """Reference ID."""
  txid: str
  """Method transaction ID."""
  info: str
  """Method transaction information."""
  amount: str
  """Amount deposited."""
  fee: NotRequired[str]
  """Fees paid. Not always present (e.g. omitted for some Ether deposits in Kraken's own documented example); type inferred as `string` by analogy with every other `fee` field in this operation family, since upstream declares no `type` for this property."""
  time: TimestampSeconds
  """Unix timestamp when the request was made."""
  status: Literal[
    'Initial', 'Pending', 'EarlyConfirmed', 'Settled', 'Success', 'Failure'
  ]
  """Status of the deposit. See the IFEX financial transaction states reference linked from upstream docs for background."""
  originators: NotRequired[list[str]]
  """Sending transaction id(s), for deposits credited via a sweeping transaction."""


DepositDepositsItemKeywords = TypedDict(
  'DepositDepositsItemKeywords',
  {'status-prop': NotRequired[Literal['return', 'onhold']]},
)
"""
- `status-prop`: Additional status property, if available: `return` for a return transaction initiated by Kraken, `onhold` when the deposit is on hold pending review.
"""


class DepositDepositsItem(DepositDepositsItemKeywords):
  """One deposit record."""

  method: str
  """Name of deposit method."""
  aclass: str
  """Asset class."""
  asset: str
  """Asset."""
  refid: str
  """Reference ID."""
  txid: str
  """Method transaction ID."""
  info: str
  """Method transaction information."""
  amount: str
  """Amount deposited."""
  fee: NotRequired[str]
  """Fees paid. Not always present (e.g. omitted for some Ether deposits in Kraken's own documented example); type inferred as `string` by analogy with every other `fee` field in this operation family, since upstream declares no `type` for this property."""
  time: TimestampSeconds
  """Unix timestamp when the request was made."""
  status: Literal[
    'Initial', 'Pending', 'EarlyConfirmed', 'Settled', 'Success', 'Failure'
  ]
  """Status of the deposit. See the IFEX financial transaction states reference linked from upstream docs for background."""
  originators: NotRequired[list[str]]
  """Sending transaction id(s), for deposits credited via a sweeping transaction."""


class Request(TypedDict):
  asset: NotRequired[str]
  """Filter for a specific asset being deposited."""
  aclass: NotRequired[Literal['currency', 'tokenized_asset']]
  """Filter for a specific asset class being deposited."""
  method: NotRequired[str]
  """Filter for a specific name of deposit method."""
  start: NotRequired[str]
  """Start timestamp; deposits created strictly before this will not be included in the response."""
  end: NotRequired[str]
  """End timestamp; deposits created strictly after this will not be included in the response."""
  cursor: NotRequired[bool | str]
  """`true`/`false` to enable/disable the paginated response shape, or a cursor string (from a previous response's `next_cursor`) to fetch the next page."""
  limit: NotRequired[int]
  """Number of results to include per page."""
  rebase_multiplier: NotRequired[Literal['rebased', 'base'] | None]
  """Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens."""


class DepositStatusPage(TypedDict):
  """Paginated deposit results, returned when the `cursor` request parameter is set."""

  deposits: list[DepositDepositsItem]
  """Page of deposit records. Confirmed live as `deposits` (plural) -- the endpoint's OpenAPI schema had documented this key as `deposit` (singular)."""
  next_cursor: NotRequired[str | None]
  """Cursor to pass back as `cursor` to fetch the next page of results. `null` once there is no further page -- confirmed live."""


Response = list[Deposit0Item] | DepositStatusPage


class DepositStatus(RpcEndpoint):
  """Retrieve information about recent deposits. Results are sorted by recency; use the `cursor` parameter to iterate through the list of deposits (page size equal to the value of `limit`) from newest to oldest. Requires the `Funds permissions - Query` API key permission."""

  @overload
  async def deposit_status(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    method: str | None = None,
    start: str | None = None,
    end: str | None = None,
    cursor: bool | str | None = None,
    limit: int | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def deposit_status(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    method: str | None = None,
    start: str | None = None,
    end: str | None = None,
    cursor: bool | str | None = None,
    limit: int | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    validate: Literal[True] | None = None,
  ) -> Response: ...
  @overload
  async def deposit_status(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    method: str | None = None,
    start: str | None = None,
    end: str | None = None,
    cursor: bool | str | None = None,
    limit: int | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    validate: bool | None = None,
  ) -> Response: ...
  async def deposit_status(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    method: str | None = None,
    start: str | None = None,
    end: str | None = None,
    cursor: bool | str | None = None,
    limit: int | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Retrieve information about recent deposits. Results are sorted by recency; use the `cursor` parameter to iterate through the list of deposits (page size equal to the value of `limit`) from newest to oldest. Requires the `Funds permissions - Query` API key permission.

    Args:
      asset: Filter for a specific asset being deposited.
      aclass: Filter for a specific asset class being deposited.
      method: Filter for a specific name of deposit method.
      start: Start timestamp; deposits created strictly before this will not be included in the response.
      end: End timestamp; deposits created strictly after this will not be included in the response.
      cursor: `true`/`false` to enable/disable the paginated response shape, or a cursor string (from a previous response's `next_cursor`) to fetch the next page.
      limit: Number of results to include per page.
      rebase_multiplier: Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/funding/get-status-of-recent-deposits)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if aclass is not None:
      request['aclass'] = aclass
    if method is not None:
      request['method'] = method
    if start is not None:
      request['start'] = start
    if end is not None:
      request['end'] = end
    if cursor is not None:
      request['cursor'] = cursor
    if limit is not None:
      request['limit'] = limit
    if rebase_multiplier is not None:
      request['rebase_multiplier'] = rebase_multiplier
    return await self.request(
      request,
      method='POST',
      path='/0/private/DepositStatus',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
