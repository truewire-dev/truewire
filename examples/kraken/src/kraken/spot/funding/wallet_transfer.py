# Generated by truewire — do not edit by hand.
from typing_extensions import Any, Literal, TypedDict, overload
from kraken.core.endpoint.rpc import RpcEndpoint


RequestKeywords = TypedDict('RequestKeywords', {'from': Literal['Spot Wallet']})
"""
- `from`: Source wallet.
"""


class Request(RequestKeywords):
  asset: str
  """Asset to transfer (asset ID or `altname`)."""
  to: Literal['Futures Wallet']
  """Destination wallet."""
  amount: str
  """Amount to transfer."""


class WalletTransferReceipt(TypedDict):
  """Reference to the created wallet transfer."""

  refid: str
  """Reference ID."""


class WalletTransfer(RpcEndpoint):
  """Transfer from a Kraken Spot wallet to a Kraken Futures wallet. A transfer in the other direction must be requested via the Kraken Futures API withdrawal-to-Spot-wallet endpoint instead. Requires the `Funds permissions - Query` API key permission."""

  @overload
  async def wallet_transfer(
    self,
    *,
    asset: str,
    from_: Literal['Spot Wallet'] = 'Spot Wallet',
    to: Literal['Futures Wallet'] = 'Futures Wallet',
    amount: str,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def wallet_transfer(
    self,
    *,
    asset: str,
    from_: Literal['Spot Wallet'] = 'Spot Wallet',
    to: Literal['Futures Wallet'] = 'Futures Wallet',
    amount: str,
    validate: Literal[True] | None = None,
  ) -> WalletTransferReceipt: ...
  @overload
  async def wallet_transfer(
    self,
    *,
    asset: str,
    from_: Literal['Spot Wallet'] = 'Spot Wallet',
    to: Literal['Futures Wallet'] = 'Futures Wallet',
    amount: str,
    validate: bool | None = None,
  ) -> WalletTransferReceipt: ...
  async def wallet_transfer(
    self,
    *,
    asset: str,
    from_: Literal['Spot Wallet'] = 'Spot Wallet',
    to: Literal['Futures Wallet'] = 'Futures Wallet',
    amount: str,
    validate: bool | None = None,
  ) -> WalletTransferReceipt:
    """Transfer from a Kraken Spot wallet to a Kraken Futures wallet. A transfer in the other direction must be requested via the Kraken Futures API withdrawal-to-Spot-wallet endpoint instead. Requires the `Funds permissions - Query` API key permission.

    Args:
      asset: Asset to transfer (asset ID or `altname`).
      from_: Source wallet.
      to: Destination wallet.
      amount: Amount to transfer.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/funding/request-wallet-transfer)
    """
    request: Request = {'asset': asset, 'from': from_, 'to': to, 'amount': amount}
    return await self.request(
      request,
      method='POST',
      path='/0/private/WalletTransfer',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=WalletTransferReceipt,
    )
