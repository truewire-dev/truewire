// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Asset being withdrawn. */
  asset: string
  /** Asset class of the asset being withdrawn. */
  aclass?: 'currency' | 'tokenized_asset'
  /** Withdrawal key name, as set up on the account. */
  key: string
  /** Optional crypto address, used to confirm the address matches `key` (returns an `Invalid withdrawal address` error if different). */
  address?: string
  /** Amount to be withdrawn. */
  amount: string
  /** Optional fee ceiling; if the processed withdrawal fee is higher than `max_fee`, the withdrawal fails with `EFunding:Max fee exceeded`. */
  max_fee?: string
  /** Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens. */
  rebase_multiplier?: 'rebased' | 'base' | null
}

export const Request: Codec<Request> = t.object({
  asset: t.string,
  aclass: t.optional(t.literal('currency', 'tokenized_asset')),
  key: t.string,
  address: t.optional(t.string),
  amount: t.string,
  max_fee: t.optional(t.string),
  rebase_multiplier: t.optional(t.nullable(t.literal('rebased', 'base'))),
})

/** Reference to the created withdrawal. */
export interface WithdrawReceipt {
  /** Reference ID. */
  refid: string
}

export const WithdrawReceipt: Codec<WithdrawReceipt> = t.object({
  refid: t.string,
})

/** Make a withdrawal request. Requires the `Funds permissions - Withdraw` API key permission. */
export class Withdraw {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  withdraw(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Make a withdrawal request. Requires the `Funds permissions - Withdraw` API key permission.
   *
   * @see https://docs.kraken.com/api-reference/funding/withdraw-funds
   */
  withdraw(request: Request, options?: CallOptions): Promise<WithdrawReceipt>
  async withdraw(request: Request, options?: CallOptions): Promise<WithdrawReceipt> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/Withdraw',
      request,
      requestCodec: Request,
      responseCodec: WithdrawReceipt,
      meta: { signed: true },
      ...options,
    })
  }
}
