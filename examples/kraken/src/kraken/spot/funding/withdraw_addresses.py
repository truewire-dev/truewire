# Generated by truewire — do not edit by hand.
from typing_extensions import Any, Literal, NotRequired, TypedDict, overload
from kraken.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  asset: NotRequired[str]
  """Filter addresses for a specific asset."""
  aclass: NotRequired[Literal['currency', 'tokenized_asset']]
  """Filter addresses for a specific asset class."""
  method: NotRequired[str]
  """Filter addresses for a specific method."""
  key: NotRequired[str]
  """Find the address set up under this withdrawal key name."""
  verified: NotRequired[bool]
  """Filter by verification status. Withdrawal addresses that have successfully completed email confirmation have a verification status of `true`."""


class WithdrawalAddress(TypedDict):
  """One withdrawal address available for the user."""

  address: NotRequired[str]
  """Withdrawal address."""
  asset: NotRequired[str]
  """Name of asset being withdrawn."""
  method: NotRequired[str]
  """Name of the withdrawal method."""
  key: NotRequired[str]
  """Withdrawal key name, as set up on the account."""
  tag: NotRequired[str]
  """Tag for XRP, or memo for STX/XLM/EOS, deposit addresses associated with this withdrawal address."""
  verified: NotRequired[bool]
  """Verification status of the withdrawal address."""


Response = list[WithdrawalAddress]


class WithdrawAddresses(RpcEndpoint):
  """Retrieve a list of withdrawal addresses available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions."""

  @overload
  async def withdraw_addresses(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    method: str | None = None,
    key: str | None = None,
    verified: bool | None = None,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def withdraw_addresses(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    method: str | None = None,
    key: str | None = None,
    verified: bool | None = None,
    validate: bool | None = None,
  ) -> Response: ...
  async def withdraw_addresses(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    method: str | None = None,
    key: str | None = None,
    verified: bool | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Retrieve a list of withdrawal addresses available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions.

    Args:
      asset: Filter addresses for a specific asset.
      aclass: Filter addresses for a specific asset class.
      method: Filter addresses for a specific method.
      key: Find the address set up under this withdrawal key name.
      verified: Filter by verification status. Withdrawal addresses that have successfully completed email confirmation have a verification status of `true`.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/funding/get-withdrawal-addresses)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if aclass is not None:
      request['aclass'] = aclass
    if method is not None:
      request['method'] = method
    if key is not None:
      request['key'] = key
    if verified is not None:
      request['verified'] = verified
    return await self.request(
      request,
      method='POST',
      path='/0/private/WithdrawAddresses',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
