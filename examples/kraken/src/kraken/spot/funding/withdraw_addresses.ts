// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Filter addresses for a specific asset. */
  asset?: string
  /** Filter addresses for a specific asset class. */
  aclass?: 'currency' | 'tokenized_asset'
  /** Filter addresses for a specific method. */
  method?: string
  /** Find the address set up under this withdrawal key name. */
  key?: string
  /** Filter by verification status. Withdrawal addresses that have successfully completed email confirmation have a verification status of `true`. */
  verified?: boolean
}

export const Request: Codec<Request> = t.object({
  asset: t.optional(t.string),
  aclass: t.optional(t.literal('currency', 'tokenized_asset')),
  method: t.optional(t.string),
  key: t.optional(t.string),
  verified: t.optional(t.boolean),
})

/** One withdrawal address available for the user. */
export interface WithdrawalAddress {
  /** Withdrawal address. */
  address?: string
  /** Name of asset being withdrawn. */
  asset?: string
  /** Name of the withdrawal method. */
  method?: string
  /** Withdrawal key name, as set up on the account. */
  key?: string
  /** Tag for XRP, or memo for STX/XLM/EOS, deposit addresses associated with this withdrawal address. */
  tag?: string
  /** Verification status of the withdrawal address. */
  verified?: boolean
}

export const WithdrawalAddress: Codec<WithdrawalAddress> = t.object({
  address: t.optional(t.string),
  asset: t.optional(t.string),
  method: t.optional(t.string),
  key: t.optional(t.string),
  tag: t.optional(t.string),
  verified: t.optional(t.boolean),
})

export type Response = WithdrawalAddress[]

export const Response: Codec<Response> = t.array(WithdrawalAddress)

/** Retrieve a list of withdrawal addresses available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions. */
export class WithdrawAddresses {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  withdrawAddresses(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Retrieve a list of withdrawal addresses available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions.
   *
   * @see https://docs.kraken.com/api-reference/funding/get-withdrawal-addresses
   */
  withdrawAddresses(request?: Request, options?: CallOptions): Promise<Response>
  async withdrawAddresses(request?: Request, options?: CallOptions): Promise<Response> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/WithdrawAddresses',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: Response,
      meta: { signed: true },
      ...options,
    })
  }
}
