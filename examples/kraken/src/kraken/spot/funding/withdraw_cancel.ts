// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Asset being withdrawn. */
  asset: string
  /** Withdrawal reference ID. */
  refid: string
}

export const Request: Codec<Request> = t.object({
  asset: t.string,
  refid: t.string,
})

export type Response = boolean

export const Response: Codec<Response> = t.boolean

/** Cancel a recently requested withdrawal, if it has not already been successfully processed. Requires the `Funds permissions - Withdraw` API key permission, unless the withdrawal is a `WalletTransfer`, in which case no permissions are required. */
export class WithdrawCancel {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  withdrawCancel(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Cancel a recently requested withdrawal, if it has not already been successfully processed. Requires the `Funds permissions - Withdraw` API key permission, unless the withdrawal is a `WalletTransfer`, in which case no permissions are required.
   *
   * @see https://docs.kraken.com/api-reference/funding/request-withdrawal-cancellation
   */
  withdrawCancel(request: Request, options?: CallOptions): Promise<Response>
  async withdrawCancel(request: Request, options?: CallOptions): Promise<Response> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/WithdrawCancel',
      request,
      requestCodec: Request,
      responseCodec: Response,
      meta: { signed: true },
      ...options,
    })
  }
}
