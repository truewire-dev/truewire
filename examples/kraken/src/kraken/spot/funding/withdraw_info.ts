// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Asset being withdrawn. */
  asset: string
  /** Withdrawal key name, as set up on the account. */
  key: string
  /** Amount to be withdrawn. */
  amount: string
}

export const Request: Codec<Request> = t.object({
  asset: t.string,
  key: t.string,
  amount: t.string,
})

/** Fee and limit information for a potential withdrawal. */
export interface WithdrawalInfo {
  /** Name of the withdrawal method that will be used. */
  method?: string
  /** Maximum net amount that can be withdrawn right now. */
  limit?: string
  /** Net amount that will be sent, after fees. */
  amount?: string
  /** Amount of fees that will be paid. */
  fee?: string
}

export const WithdrawalInfo: Codec<WithdrawalInfo> = t.object({
  method: t.optional(t.string),
  limit: t.optional(t.string),
  amount: t.optional(t.string),
  fee: t.optional(t.string),
})

/** Retrieve fee information about a potential withdrawal for a particular asset, key, and amount. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions. */
export class WithdrawInfo {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  withdrawInfo(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Retrieve fee information about a potential withdrawal for a particular asset, key, and amount. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions.
   *
   * @see https://docs.kraken.com/api-reference/funding/get-withdrawal-information
   */
  withdrawInfo(request: Request, options?: CallOptions): Promise<WithdrawalInfo>
  async withdrawInfo(request: Request, options?: CallOptions): Promise<WithdrawalInfo> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/WithdrawInfo',
      request,
      requestCodec: Request,
      responseCodec: WithdrawalInfo,
      meta: { signed: true },
      ...options,
    })
  }
}
