# Generated by truewire — do not edit by hand.
from decimal import Decimal
from typing_extensions import Literal, NotRequired, TypedDict
from kraken.core.endpoint.rpc import RpcEndpoint


class Request(TypedDict):
  asset: NotRequired[str]
  """Filter methods for a specific asset."""
  aclass: NotRequired[Literal['currency', 'tokenized_asset']]
  """Filter methods for a specific asset class."""
  network: NotRequired[str]
  """Filter methods for a specific network."""
  rebase_multiplier: NotRequired[Literal['rebased', 'base'] | None]
  """Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens."""


class WithdrawalMethodFee(TypedDict):
  """Fee details for a withdrawal method."""

  aclass: NotRequired[str]
  """Asset class of the fee."""
  asset: NotRequired[str]
  """Asset used for the fee."""
  fee: Decimal
  """Flat fee amount."""
  fee_percentage: NotRequired[Decimal]
  """Percentage fee (present instead of a flat fee for some methods)."""


class WithdrawalRateLimitWindow(TypedDict):
  """Rate limit usage for one time window."""

  maximum: NotRequired[str]
  """Maximum amount allowed in this window."""
  remaining: NotRequired[str]
  """Amount remaining in this window."""
  used: NotRequired[str]
  """Amount already used in this window."""


class WithdrawalRateLimit(TypedDict):
  """One rate limit rule for a withdrawal method."""

  description: NotRequired[str]
  """Description of this rate limit."""
  limit_type: NotRequired[str]
  """Type of rate limit."""
  limits: NotRequired[dict[str, WithdrawalRateLimitWindow]]
  """Limits keyed by the time window they apply to, in seconds."""


class WithdrawalMethod(TypedDict):
  """One withdrawal method available for the user."""

  asset: NotRequired[str]
  """Name of asset being withdrawn."""
  method: NotRequired[str]
  """Name of the withdrawal method."""
  method_id: NotRequired[str]
  """Unique identifier for the withdrawal method."""
  network: NotRequired[str]
  """Name of the blockchain or network being withdrawn on."""
  network_id: NotRequired[str]
  """Unique identifier for the network."""
  minimum: NotRequired[Decimal]
  """Minimum net amount that can be withdrawn right now."""
  fee: WithdrawalMethodFee
  limits: NotRequired[list[WithdrawalRateLimit]]
  """Rate limit details for this withdrawal method."""


Response = list[WithdrawalMethod]


class WithdrawMethods(RpcEndpoint):
  """Retrieve a list of withdrawal methods available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions."""

  async def withdraw_methods(
    self,
    *,
    asset: str | None = None,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    network: str | None = None,
    rebase_multiplier: Literal['rebased', 'base'] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Retrieve a list of withdrawal methods available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions.

    Args:
      asset: Filter methods for a specific asset.
      aclass: Filter methods for a specific asset class.
      network: Filter methods for a specific network.
      rebase_multiplier: Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/funding/get-withdrawal-methods)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if aclass is not None:
      request['aclass'] = aclass
    if network is not None:
      request['network'] = network
    if rebase_multiplier is not None:
      request['rebase_multiplier'] = rebase_multiplier
    return await self.request(
      request,
      method='POST',
      path='/0/private/WithdrawMethods',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
