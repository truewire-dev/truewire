// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type Decimal, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Filter methods for a specific asset. */
  asset?: string
  /** Filter methods for a specific asset class. */
  aclass?: 'currency' | 'tokenized_asset'
  /** Filter methods for a specific network. */
  network?: string
  /** Optional parameter for viewing xstocks data. `rebased` displays in terms of underlying equity; `base` displays in terms of SPV tokens. */
  rebase_multiplier?: 'rebased' | 'base' | null
}

export const Request: Codec<Request> = t.object({
  asset: t.optional(t.string),
  aclass: t.optional(t.literal('currency', 'tokenized_asset')),
  network: t.optional(t.string),
  rebase_multiplier: t.optional(t.nullable(t.literal('rebased', 'base'))),
})

/** Fee details for a withdrawal method. */
export interface WithdrawalMethodFee {
  /** Asset class of the fee. */
  aclass?: string
  /** Asset used for the fee. */
  asset?: string
  /** Flat fee amount. */
  fee: Decimal
  /** Percentage fee (present instead of a flat fee for some methods). */
  fee_percentage?: Decimal
}

export const WithdrawalMethodFee: Codec<WithdrawalMethodFee> = t.object({
  aclass: t.optional(t.string),
  asset: t.optional(t.string),
  fee: t.decimal,
  fee_percentage: t.optional(t.decimal),
})

/** Rate limit usage for one time window. */
export interface WithdrawalRateLimitWindow {
  /** Maximum amount allowed in this window. */
  maximum?: string
  /** Amount remaining in this window. */
  remaining?: string
  /** Amount already used in this window. */
  used?: string
}

export const WithdrawalRateLimitWindow: Codec<WithdrawalRateLimitWindow> = t.object({
  maximum: t.optional(t.string),
  remaining: t.optional(t.string),
  used: t.optional(t.string),
})

/** One rate limit rule for a withdrawal method. */
export interface WithdrawalRateLimit {
  /** Description of this rate limit. */
  description?: string
  /** Type of rate limit. */
  limit_type?: string
  /** Limits keyed by the time window they apply to, in seconds. */
  limits?: Record<string, WithdrawalRateLimitWindow>
}

export const WithdrawalRateLimit: Codec<WithdrawalRateLimit> = t.object({
  description: t.optional(t.string),
  limit_type: t.optional(t.string),
  limits: t.optional(t.record(WithdrawalRateLimitWindow)),
})

/** One withdrawal method available for the user. */
export interface WithdrawalMethod {
  /** Name of asset being withdrawn. */
  asset?: string
  /** Name of the withdrawal method. */
  method?: string
  /** Unique identifier for the withdrawal method. */
  method_id?: string
  /** Name of the blockchain or network being withdrawn on. */
  network?: string
  /** Unique identifier for the network. */
  network_id?: string
  /** Minimum net amount that can be withdrawn right now. */
  minimum?: Decimal
  fee: WithdrawalMethodFee
  /** Rate limit details for this withdrawal method. */
  limits?: WithdrawalRateLimit[]
}

export const WithdrawalMethod: Codec<WithdrawalMethod> = t.object({
  asset: t.optional(t.string),
  method: t.optional(t.string),
  method_id: t.optional(t.string),
  network: t.optional(t.string),
  network_id: t.optional(t.string),
  minimum: t.optional(t.decimal),
  fee: WithdrawalMethodFee,
  limits: t.optional(t.array(WithdrawalRateLimit)),
})

export type Response = WithdrawalMethod[]

export const Response: Codec<Response> = t.array(WithdrawalMethod)

/** Retrieve a list of withdrawal methods available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions. */
export class WithdrawMethods {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  withdrawMethods(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Retrieve a list of withdrawal methods available for the user. Requires the `Funds permissions - Query` and `Funds permissions - Withdraw` API key permissions.
   *
   * @see https://docs.kraken.com/api-reference/funding/get-withdrawal-methods
   */
  withdrawMethods(request?: Request, options?: CallOptions): Promise<Response>
  async withdrawMethods(request?: Request, options?: CallOptions): Promise<Response> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/WithdrawMethods',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: Response,
      meta: { signed: true },
      ...options,
    })
  }
}
