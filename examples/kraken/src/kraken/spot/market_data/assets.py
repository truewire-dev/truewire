# Generated by truewire — do not edit by hand.
from typing_extensions import Any, Literal, NotRequired, TypedDict, overload
from kraken.core.endpoint.rpc import RpcEndpoint


class AssetInfo(TypedDict):
  """One asset's deposit/withdrawal/trading/earn info."""

  aclass: NotRequired[str]
  """Asset class."""
  altname: NotRequired[str]
  """Alternate name."""
  decimals: NotRequired[int]
  """Number of decimal places for record keeping amounts of this asset."""
  display_decimals: NotRequired[int]
  """Number of decimal places shown for display purposes in frontends."""
  collateral_value: NotRequired[float]
  """Valuation as margin collateral, if applicable."""
  status: NotRequired[
    Literal[
      'enabled', 'deposit_only', 'withdrawal_only', 'funding_temporarily_disabled'
    ]
  ]
  """Status of the asset."""
  margin_rate: NotRequired[str]
  """Interest rate charged when borrowing this asset for margin trading, as a decimal string."""


class Request(TypedDict):
  asset: NotRequired[str]
  """Comma delimited list of assets to get info on (optional, default all available assets)."""
  aclass: NotRequired[Literal['currency', 'tokenized_asset']]
  """Filters the asset class to retrieve: `currency` (spot currency pairs) or `tokenized_asset` (xstocks)."""
  assetVersion: NotRequired[Literal[1]]
  """Controls whether response keys and asset identifier fields use Kraken's internal names or display names. Omitted (default): internal names are used, asset keys use the legacy `X`/`Z`-prefixed format (`XXBT`, `ZUSD`). `assetVersion=1`: display names are used, asset keys become their canonical display names (`BTC`, `USD`). Only `assetVersion=1` is currently supported. The `altname` field is not affected by this parameter."""


Response = dict[str, AssetInfo]


class Assets(RpcEndpoint):
  """Get information about the assets that are available for deposit, withdrawal, trading and earn."""

  @overload
  async def assets(
    self,
    asset: str | None = None,
    *,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    asset_version: Literal[1] | None = None,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def assets(
    self,
    asset: str | None = None,
    *,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    asset_version: Literal[1] | None = None,
    validate: bool | None = None,
  ) -> Response: ...
  async def assets(
    self,
    asset: str | None = None,
    *,
    aclass: Literal['currency', 'tokenized_asset'] | None = None,
    asset_version: Literal[1] | None = None,
    validate: bool | None = None,
  ) -> Response:
    """Get information about the assets that are available for deposit, withdrawal, trading and earn.

    Args:
      asset: Comma delimited list of assets to get info on (optional, default all available assets).
      aclass: Filters the asset class to retrieve: `currency` (spot currency pairs) or `tokenized_asset` (xstocks).
      asset_version: Controls whether response keys and asset identifier fields use Kraken's internal names or display names. Omitted (default): internal names are used, asset keys use the legacy `X`/`Z`-prefixed format (`XXBT`, `ZUSD`). `assetVersion=1`: display names are used, asset keys become their canonical display names (`BTC`, `USD`). Only `assetVersion=1` is currently supported. The `altname` field is not affected by this parameter.
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/market-data/get-asset-info)
    """
    request: Request = Request()
    if asset is not None:
      request['asset'] = asset
    if aclass is not None:
      request['aclass'] = aclass
    if asset_version is not None:
      request['assetVersion'] = asset_version
    return await self.request(
      request,
      method='GET',
      path='/0/public/Assets',
      meta={},
      validate=validate,
      request_type=Request,
      response_type=Response,
    )
