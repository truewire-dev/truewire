// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** One asset's deposit/withdrawal/trading/earn info. */
export interface AssetInfo {
  /** Asset class. */
  aclass?: string
  /** Alternate name. */
  altname?: string
  /** Number of decimal places for record keeping amounts of this asset. */
  decimals?: number
  /** Number of decimal places shown for display purposes in frontends. */
  display_decimals?: number
  /** Valuation as margin collateral, if applicable. */
  collateral_value?: number
  /** Status of the asset. */
  status?: 'enabled' | 'deposit_only' | 'withdrawal_only' | 'funding_temporarily_disabled'
  /** Interest rate charged when borrowing this asset for margin trading, as a decimal string. */
  margin_rate?: string
}

export const AssetInfo: Codec<AssetInfo> = t.object({
  aclass: t.optional(t.string),
  altname: t.optional(t.string),
  decimals: t.optional(t.integer),
  display_decimals: t.optional(t.integer),
  collateral_value: t.optional(t.number),
  status: t.optional(t.literal('enabled', 'deposit_only', 'withdrawal_only', 'funding_temporarily_disabled')),
  margin_rate: t.optional(t.string),
})

export interface Request {
  /** Comma delimited list of assets to get info on (optional, default all available assets). */
  asset?: string
  /** Filters the asset class to retrieve: `currency` (spot currency pairs) or `tokenized_asset` (xstocks). */
  aclass?: 'currency' | 'tokenized_asset'
  /** Controls whether response keys and asset identifier fields use Kraken's internal names or display names. Omitted (default): internal names are used, asset keys use the legacy `X`/`Z`-prefixed format (`XXBT`, `ZUSD`). `assetVersion=1`: display names are used, asset keys become their canonical display names (`BTC`, `USD`). Only `assetVersion=1` is currently supported. The `altname` field is not affected by this parameter. */
  assetVersion?: 1
}

export const Request: Codec<Request> = t.object({
  asset: t.optional(t.string),
  aclass: t.optional(t.literal('currency', 'tokenized_asset')),
  assetVersion: t.optional(t.literal(1)),
})

export type Response = Record<string, AssetInfo>

export const Response: Codec<Response> = t.record(AssetInfo)

/** Get information about the assets that are available for deposit, withdrawal, trading and earn. */
export class Assets {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  assets(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Get information about the assets that are available for deposit, withdrawal, trading and earn.
   *
   * @see https://docs.kraken.com/api-reference/market-data/get-asset-info
   */
  assets(request?: Request, options?: CallOptions): Promise<Response>
  async assets(request?: Request, options?: CallOptions): Promise<Response> {
    return this.core.request({
      method: 'GET',
      path: '/0/public/Assets',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: Response,
      meta: {},
      ...options,
    })
  }
}
