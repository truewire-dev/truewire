// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, type TimestampSeconds, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Asset pair to get data for. */
  pair: string
  /** Controls whether response keys use Kraken's internal names or display names. Omitted (default): internal names are used. `assetVersion=1`: display names are used. Only `assetVersion=1` is currently supported. */
  assetVersion?: 1
  /** Returns spread data since given timestamp. Optional, intended for incremental updates within available dataset (does not contain all historical spreads). */
  since?: TimestampSeconds
  /** This parameter is required on requests for non-crypto pairs, i.e. use `tokenized_asset` for xstocks. */
  asset_class?: 'tokenized_asset'
}

export const Request: Codec<Request> = t.object({
  pair: t.string,
  assetVersion: t.optional(t.literal(1)),
  since: t.optional(t.epochSeconds),
  asset_class: t.optional(t.literal('tokenized_asset')),
})

/** Spreads keyed by pair, plus the polling cursor. */
export interface SpreadResult {
  /** ID to be used as `since` when polling for new spread data. */
  last?: number
}

export const SpreadResult: Codec<SpreadResult> = t.object({
  last: t.optional(t.integer),
})

/** Returns the last ~200 top-of-book spreads for a given pair. */
export class Spread {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  spread(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Returns the last ~200 top-of-book spreads for a given pair.
   *
   * @see https://docs.kraken.com/api-reference/market-data/get-recent-spreads
   */
  spread(request: Request, options?: CallOptions): Promise<SpreadResult>
  async spread(request: Request, options?: CallOptions): Promise<SpreadResult> {
    return this.core.request({
      method: 'GET',
      path: '/0/public/Spread',
      request,
      requestCodec: Request,
      responseCodec: SpreadResult,
      meta: {},
      ...options,
    })
  }
}
