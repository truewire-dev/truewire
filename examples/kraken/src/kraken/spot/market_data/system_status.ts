// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, type TimestampIso, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** The exchange's current operational status. */
export interface SystemStatus {
  /** Current system status: `online` (operating normally, all order types accepted), `maintenance` (offline, no new orders or cancellations), `cancel_only` (resting orders can be cancelled but none submitted, no trades occur), or `post_only` (only post-only limit orders accepted, existing orders may still be cancelled, no trades occur). */
  status?: 'online' | 'maintenance' | 'cancel_only' | 'post_only'
  /** Current timestamp (RFC3339). */
  timestamp?: TimestampIso
}

export const SystemStatus: Codec<SystemStatus> = t.object({
  status: t.optional(t.literal('online', 'maintenance', 'cancel_only', 'post_only')),
  timestamp: t.optional(t.dateTime),
})

/** Get the current system status or trading mode. */
export class SystemStatusEndpoint {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  systemStatus(options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Get the current system status or trading mode.
   *
   * @see https://docs.kraken.com/api-reference/market-data/get-system-status
   */
  systemStatus(options?: CallOptions): Promise<SystemStatus>
  async systemStatus(options?: CallOptions): Promise<SystemStatus> {
    return this.core.request({
      method: 'GET',
      path: '/0/public/SystemStatus',
      request: undefined,
      requestCodec: undefined,
      responseCodec: SystemStatus,
      meta: {},
      ...options,
    })
  }
}
