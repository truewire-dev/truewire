// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** One pair's ticker snapshot. Field names follow Kraken's own wire shortcuts. */
export interface AssetTicker {
  /** Ask `[price, whole lot volume, lot volume]`. */
  a?: readonly [string, string, string]
  /** Bid `[price, whole lot volume, lot volume]`. */
  b?: readonly [string, string, string]
  /** Last trade closed `[price, lot volume]`. */
  c?: readonly [string, string]
  /** Volume `[today, last 24 hours]`. */
  v?: readonly [string, string]
  /** Volume weighted average price `[today, last 24 hours]`. */
  p?: readonly [string, string]
  /** Number of trades `[today, last 24 hours]`. */
  t?: readonly [number, number]
  /** Low `[today, last 24 hours]`. */
  l?: readonly [string, string]
  /** High `[today, last 24 hours]`. */
  h?: readonly [string, string]
  /** Today's opening price. */
  o?: string
}

export const AssetTicker: Codec<AssetTicker> = t.object({
  a: t.optional(t.tuple([t.string, t.string, t.string])),
  b: t.optional(t.tuple([t.string, t.string, t.string])),
  c: t.optional(t.tuple([t.string, t.string])),
  v: t.optional(t.tuple([t.string, t.string])),
  p: t.optional(t.tuple([t.string, t.string])),
  t: t.optional(t.tuple([t.integer, t.integer])),
  l: t.optional(t.tuple([t.string, t.string])),
  h: t.optional(t.tuple([t.string, t.string])),
  o: t.optional(t.string),
})

export interface Request {
  /** Asset pair to get data for (optional, default: all tradeable exchange pairs). */
  pair?: string
  /** Controls whether response keys and asset identifier fields use Kraken's internal names or display names. Omitted (default): internal names are used. `assetVersion=1`: display names are used. Only `assetVersion=1` is currently supported. */
  assetVersion?: 1
  /** This parameter is required on requests for tokenized pairs, i.e. xstocks. If `asset_class` is provided without the `pair` parameter, all pairs for that asset class will be returned. */
  asset_class?: 'tokenized_asset' | 'forex'
}

export const Request: Codec<Request> = t.object({
  pair: t.optional(t.string),
  assetVersion: t.optional(t.literal(1)),
  asset_class: t.optional(t.literal('tokenized_asset', 'forex')),
})

export type Response = Record<string, AssetTicker>

export const Response: Codec<Response> = t.record(AssetTicker)

/** Get ticker information for all or requested markets. Today's prices start at midnight UTC. Leaving the `pair` parameter blank returns tickers for all tradeable assets on Kraken. */
export class Ticker {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  ticker(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Get ticker information for all or requested markets. Today's prices start at midnight UTC. Leaving the `pair` parameter blank returns tickers for all tradeable assets on Kraken.
   *
   * @see https://docs.kraken.com/api-reference/market-data/get-ticker-information
   */
  ticker(request?: Request, options?: CallOptions): Promise<Response>
  async ticker(request?: Request, options?: CallOptions): Promise<Response> {
    return this.core.request({
      method: 'GET',
      path: '/0/public/Ticker',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: Response,
      meta: {},
      ...options,
    })
  }
}
