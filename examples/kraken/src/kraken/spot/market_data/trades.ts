// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, type TimestampNanos, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

export interface Request {
  /** Asset pair to get data for. */
  pair: string
  /** Controls whether response keys use Kraken's internal names or display names. Omitted (default): internal names are used. `assetVersion=1`: display names are used. Only `assetVersion=1` is currently supported. */
  assetVersion?: 1
  /** Return trade data since given timestamp. */
  since?: TimestampNanos
  /** Return specific number of trades, up to 1000. */
  count?: number
  /** This parameter is required on requests for non-crypto pairs, i.e. use `tokenized_asset` for xstocks. */
  asset_class?: 'tokenized_asset'
}

export const Request: Codec<Request> = t.object({
  pair: t.string,
  assetVersion: t.optional(t.literal(1)),
  since: t.optional(t.epochNanos),
  count: t.optional(t.integer),
  asset_class: t.optional(t.literal('tokenized_asset')),
})

/** Trades keyed by pair, plus the polling cursor. */
export interface TradesResult {
  /** ID to be used as `since` when polling for new trade data. */
  last?: TimestampNanos
}

export const TradesResult: Codec<TradesResult> = t.object({
  last: t.optional(t.epochNanos),
})

/** Returns the last 1000 trades by default. */
export class Trades {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  trades(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Returns the last 1000 trades by default.
   *
   * @see https://docs.kraken.com/api-reference/market-data/get-recent-trades
   */
  trades(request: Request, options?: CallOptions): Promise<TradesResult>
  async trades(request: Request, options?: CallOptions): Promise<TradesResult> {
    return this.core.request({
      method: 'GET',
      path: '/0/public/Trades',
      request,
      requestCodec: Request,
      responseCodec: TradesResult,
      meta: {},
      ...options,
    })
  }
}
