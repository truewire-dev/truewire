# Generated by truewire — do not edit by hand.
from truewire_core.types import TimestampIso
from typing_extensions import Any, Literal, NotRequired, TypedDict, overload
from kraken.core.endpoint.rpc import RpcEndpoint
from kraken.schemas import ConditionalCloseOrder


class AddOrderBatchFailure(TypedDict):
  """An order from the batch that was rejected during pre-match checks."""

  error: NotRequired[str]
  """Error description for this individual order."""


class OrderDescription(TypedDict):
  """Human-readable description of the order."""

  order: NotRequired[str]
  """Human-readable order description."""


class AddOrderBatchSuccess(TypedDict):
  """An order from the batch that was accepted."""

  txid: NotRequired[str]
  """Transaction ID for this order."""
  descr: NotRequired[OrderDescription]


class BatchOrderIceberg(TypedDict):
  """An iceberg order within an AddOrderBatch request: a limit order that shows only `displayvol` of its quantity in the book while the rest remains hidden."""

  ordertype: Literal['iceberg']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Limit price for the order. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""
  displayvol: str
  """Quantity to show in the book while the rest of the order quantity (`volume`) remains hidden. Minimum value is 1/15 of `volume`."""


class BatchOrderLimit(TypedDict):
  """A limit order within an AddOrderBatch request: rests in the book at `price` or better."""

  ordertype: Literal['limit']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Limit price for the order. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""


class BatchOrderMarket(TypedDict):
  """A market order within an AddOrderBatch request: executes immediately at the best available price."""

  ordertype: Literal['market']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""


class BatchOrderSettlePosition(TypedDict):
  """Settles an open leveraged position at the current market price, within an AddOrderBatch request."""

  ordertype: Literal['settle-position']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""


class BatchOrderStopLoss(TypedDict):
  """A stop-loss order within an AddOrderBatch request: becomes a market order once `price` (the trigger) is reached in an unfavourable direction."""

  ordertype: Literal['stop-loss']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Trigger price for the order. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""
  trigger: NotRequired[Literal['index', 'last']]
  """Price signal used to trigger this order (and any attached conditional close order). During connectivity issues with external index feeds, the last trade price is used as a fallback reference."""


class BatchOrderStopLossLimit(TypedDict):
  """A stop-loss-limit order within an AddOrderBatch request: becomes a limit order at `price2` once `price` (the trigger) is reached in an unfavourable direction."""

  ordertype: Literal['stop-loss-limit']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Trigger price for the order. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""
  price2: str
  """Secondary limit price for the order once triggered. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""
  trigger: NotRequired[Literal['index', 'last']]
  """Price signal used to trigger this order (and any attached conditional close order). During connectivity issues with external index feeds, the last trade price is used as a fallback reference."""


class BatchOrderTakeProfit(TypedDict):
  """A take-profit order within an AddOrderBatch request: becomes a market order once `price` (the trigger) is reached in a favourable direction."""

  ordertype: Literal['take-profit']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Trigger price for the order. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""
  trigger: NotRequired[Literal['index', 'last']]
  """Price signal used to trigger this order (and any attached conditional close order). During connectivity issues with external index feeds, the last trade price is used as a fallback reference."""


class BatchOrderTakeProfitLimit(TypedDict):
  """A take-profit-limit order within an AddOrderBatch request: becomes a limit order at `price2` once `price` (the trigger) is reached in a favourable direction."""

  ordertype: Literal['take-profit-limit']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Trigger price for the order. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""
  price2: str
  """Secondary limit price for the order once triggered. May be prefixed with `+`/`-` (offset from last traded price) or `#` (add or subtract depending on order direction), and suffixed with `%` for a relative percentage instead of an absolute difference."""
  trigger: NotRequired[Literal['index', 'last']]
  """Price signal used to trigger this order (and any attached conditional close order). During connectivity issues with external index feeds, the last trade price is used as a fallback reference."""


class BatchOrderTrailingStop(TypedDict):
  """A trailing-stop order within an AddOrderBatch request: triggers as a market order once the price reverts by `price` from its peak/trough since order entry."""

  ordertype: Literal['trailing-stop']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Relative trailing offset from the last traded price. Must use the `+` prefix (direction is inferred from the order's buy/sell side), and also accepts the `%` suffix."""
  trigger: NotRequired[Literal['index', 'last']]
  """Price signal used to trigger this order (and any attached conditional close order). During connectivity issues with external index feeds, the last trade price is used as a fallback reference."""


class BatchOrderTrailingStopLimit(TypedDict):
  """A trailing-stop-limit order within an AddOrderBatch request: triggers a limit order at an offset (`price2`) from the trigger price once the price reverts by `price` from its peak/trough since order entry."""

  ordertype: Literal['trailing-stop-limit']
  """The execution model of the order."""
  type: Literal['buy', 'sell']
  """Order direction."""
  volume: str
  """Order quantity in terms of the base asset. May be `0` for closing margin orders to automatically fill the requisite quantity."""
  userref: NotRequired[int]
  """An optional non-unique numeric identifier the client can associate with any number of orders (e.g. for grouping by pair/side/strategy), letting cancel/query calls target the group by `userref` instead of individual `txid`s. Uniqueness is not enforced. Mutually exclusive with `cl_ord_id`."""
  cl_ord_id: NotRequired[str]
  """An alphanumeric client order identifier which uniquely identifies this order for the client. One of: a long UUID (`6d1b345e-2821-40e2-ad83-4ecb18a06876`), a short UUID with no dashes, or free-form ASCII text up to 18 characters. Mutually exclusive with `userref`."""
  leverage: NotRequired[str]
  """Amount of leverage desired. Defaults to none."""
  reduce_only: NotRequired[bool]
  """If true, the order will only reduce a currently open position rather than increase it or open a new one."""
  stptype: NotRequired[Literal['cancel-newest', 'cancel-oldest', 'cancel-both']]
  """Self Trade Prevention (STP) mode, defining which order(s) are expired to prevent a self-match: `cancel-newest` cancels the arriving order, `cancel-oldest` cancels the resting order, `cancel-both` cancels both."""
  oflags: NotRequired[str]
  """Comma-delimited list of order flags: `post` (post-only, available when `ordertype = limit`), `fcib` (prefer fee in base currency, default when selling), `fciq` (prefer fee in quote currency, default when buying, mutually exclusive with `fcib`), `nompp` (deprecated -- disabling Market Price Protection is no longer supported; accepted but ignored if supplied), `viqc` (order volume expressed in quote currency; buy market orders only, unavailable on margin orders)."""
  timeinforce: NotRequired[Literal['GTC', 'IOC', 'GTD']]
  """How long the order should remain in the book before being cancelled. `GTC` (good-'til-cancelled, default): rests until filled or cancelled. `IOC` (immediate-or-cancel): fills what it can immediately, cancels the remainder rather than resting. `GTD` (good-'til-date): must be paired with `expiretm`."""
  starttm: NotRequired[str]
  """Scheduled start time: `0` for now (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now."""
  expiretm: NotRequired[str]
  """Expiry time for `GTD` orders, up to one month in the future: `0` for no expiration (default), an absolute unix timestamp, or `+<n>` for `<n>` seconds from now, minimum 5."""
  close: NotRequired[ConditionalCloseOrder | None]
  """Conditional close order, attached to this order. Triggered by execution of the primary order in the same quantity and opposite direction, but once triggered becomes an independent order that may reduce or increase net position, not just close it."""
  price: str
  """Relative trailing offset from the last traded price. Must use the `+` prefix (direction is inferred from the order's buy/sell side), and also accepts the `%` suffix."""
  price2: str
  """Relative offset (`+`/`-` prefix, optionally `%` suffix) from the trigger price to the limit price -- `+0` sets the limit price equal to the trigger price."""
  trigger: NotRequired[Literal['index', 'last']]
  """Price signal used to trigger this order (and any attached conditional close order). During connectivity issues with external index feeds, the last trade price is used as a fallback reference."""


class OrderBatchAdded(TypedDict):
  """Result of submitting an order batch."""

  orders: NotRequired[list[AddOrderBatchSuccess | AddOrderBatchFailure]]
  """One result per submitted order, in request order."""


class Request(TypedDict):
  """Parameters for placing a batch of new Spot orders on a single pair."""

  orders: list[
    BatchOrderMarket
    | BatchOrderLimit
    | BatchOrderIceberg
    | BatchOrderStopLoss
    | BatchOrderTakeProfit
    | BatchOrderStopLossLimit
    | BatchOrderTakeProfitLimit
    | BatchOrderTrailingStop
    | BatchOrderTrailingStopLimit
    | BatchOrderSettlePosition
  ]
  """Orders to submit, 2 to 15 entries, all on `pair`."""
  pair: str
  """Asset pair `id` or `altname` shared by every order in the batch."""
  asset_class: NotRequired[Literal['tokenized_asset']]
  """Required on requests for non-crypto pairs, e.g. `tokenized_asset` for xstocks."""
  deadline: NotRequired[TimestampIso]
  """RFC3339 timestamp (e.g. `2021-04-01T00:18:45Z`) after which the matching engine should reject this batch if it is still queued due to latency, minimum now()+2s, maximum now()+60s."""
  validate: NotRequired[bool]
  """If true, orders are validated only and never reach the matching engine."""
  broker: NotRequired[str | None]
  """Broker IIBAN (partner's Kraken IIBAN)."""


class AddOrderBatch(RpcEndpoint):
  """Send a batch of 2 to 15 orders, all for a single trading pair. The whole batch is validated before submission -- one invalid order rejects the batch. Once submitted, individual orders that fail pre-match checks (e.g. insufficient funds) are rejected individually and the rest of the batch is still processed.

  **API Key Permissions Required:** `Orders and trades - Create & modify orders` and `Orders and trades - Cancel & close orders`"""

  @overload
  async def add_order_batch(
    self,
    orders: list[
      BatchOrderMarket
      | BatchOrderLimit
      | BatchOrderIceberg
      | BatchOrderStopLoss
      | BatchOrderTakeProfit
      | BatchOrderStopLossLimit
      | BatchOrderTakeProfitLimit
      | BatchOrderTrailingStop
      | BatchOrderTrailingStopLimit
      | BatchOrderSettlePosition
    ],
    *,
    pair: str,
    asset_class: Literal['tokenized_asset'] | None = None,
    deadline: TimestampIso | None = None,
    validate_: bool | None = None,
    broker: str | None = None,
    validate: Literal[False],
  ) -> Any: ...
  @overload
  async def add_order_batch(
    self,
    orders: list[
      BatchOrderMarket
      | BatchOrderLimit
      | BatchOrderIceberg
      | BatchOrderStopLoss
      | BatchOrderTakeProfit
      | BatchOrderStopLossLimit
      | BatchOrderTakeProfitLimit
      | BatchOrderTrailingStop
      | BatchOrderTrailingStopLimit
      | BatchOrderSettlePosition
    ],
    *,
    pair: str,
    asset_class: Literal['tokenized_asset'] | None = None,
    deadline: TimestampIso | None = None,
    validate_: bool | None = None,
    broker: str | None = None,
    validate: Literal[True] | None = None,
  ) -> OrderBatchAdded: ...
  @overload
  async def add_order_batch(
    self,
    orders: list[
      BatchOrderMarket
      | BatchOrderLimit
      | BatchOrderIceberg
      | BatchOrderStopLoss
      | BatchOrderTakeProfit
      | BatchOrderStopLossLimit
      | BatchOrderTakeProfitLimit
      | BatchOrderTrailingStop
      | BatchOrderTrailingStopLimit
      | BatchOrderSettlePosition
    ],
    *,
    pair: str,
    asset_class: Literal['tokenized_asset'] | None = None,
    deadline: TimestampIso | None = None,
    validate_: bool | None = None,
    broker: str | None = None,
    validate: bool | None = None,
  ) -> OrderBatchAdded: ...
  async def add_order_batch(
    self,
    orders: list[
      BatchOrderMarket
      | BatchOrderLimit
      | BatchOrderIceberg
      | BatchOrderStopLoss
      | BatchOrderTakeProfit
      | BatchOrderStopLossLimit
      | BatchOrderTakeProfitLimit
      | BatchOrderTrailingStop
      | BatchOrderTrailingStopLimit
      | BatchOrderSettlePosition
    ],
    *,
    pair: str,
    asset_class: Literal['tokenized_asset'] | None = None,
    deadline: TimestampIso | None = None,
    validate_: bool | None = None,
    broker: str | None = None,
    validate: bool | None = None,
  ) -> OrderBatchAdded:
    """Send a batch of 2 to 15 orders, all for a single trading pair. The whole batch is validated before submission -- one invalid order rejects the batch. Once submitted, individual orders that fail pre-match checks (e.g. insufficient funds) are rejected individually and the rest of the batch is still processed.

    **API Key Permissions Required:** `Orders and trades - Create & modify orders` and `Orders and trades - Cancel & close orders`

    Args:
      orders: Orders to submit, 2 to 15 entries, all on `pair`.
      pair: Asset pair `id` or `altname` shared by every order in the batch.
      asset_class: Required on requests for non-crypto pairs, e.g. `tokenized_asset` for xstocks.
      deadline: RFC3339 timestamp (e.g. `2021-04-01T00:18:45Z`) after which the matching engine should reject this batch if it is still queued due to latency, minimum now()+2s, maximum now()+60s.
      validate_: If true, orders are validated only and never reach the matching engine.
      broker: Broker IIBAN (partner's Kraken IIBAN).
      validate: Override this call's response validation; falls back to the client-level default when omitted. `False` returns the parsed body as it came, typed `Any`.

    References:
      - [Official docs](https://docs.kraken.com/api-reference/trading/add-order-batch)
    """
    request: Request = Request(orders=orders, pair=pair)
    if asset_class is not None:
      request['asset_class'] = asset_class
    if deadline is not None:
      request['deadline'] = deadline
    if validate_ is not None:
      request['validate'] = validate_
    if broker is not None:
      request['broker'] = broker
    return await self.request(
      request,
      method='POST',
      path='/0/private/AddOrderBatch',
      meta={'signed': True},
      validate=validate,
      request_type=Request,
      response_type=OrderBatchAdded,
    )
