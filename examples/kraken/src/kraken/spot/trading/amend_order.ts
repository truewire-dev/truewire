// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, type TimestampIso, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** Result of amending a live order. */
export interface OrderAmended {
  /** The unique Kraken identifier generated for this amend transaction. */
  amend_id?: string
}

export const OrderAmended: Codec<OrderAmended> = t.object({
  amend_id: t.optional(t.string),
})

/** Parameters for amending a live order in place. */
export interface Request {
  /** The Kraken identifier for the order to amend. Either `txid` or `cl_ord_id` is required. */
  txid?: string
  /** The client identifier for the order to amend. Either `txid` or `cl_ord_id` is required. */
  cl_ord_id?: string
  /** The new order quantity, in terms of the base asset. */
  order_qty?: string
  /** For `iceberg` orders only, the new quantity to show in the book while the rest of the order quantity remains hidden. Minimum value is 1/15 of the remaining order quantity. */
  display_qty?: string
  /** The new limit price (for order types that support a limit price). May use relative pricing: `+`/`-` prefix as an offset from the reference price (e.g. `+50` or `-100`), optionally suffixed with `%`. */
  limit_price?: string
  /** The new trigger price (for triggered order types only). May use relative pricing: `+`/`-` prefix as an offset from the reference price, optionally suffixed with `%`. */
  trigger_price?: string
  /** Required on amends for non-crypto pairs, e.g. the pair symbol for xstocks. */
  pair?: string
  /** For `limit_price` amends only. If true, the limit price change is rejected unless it can be posted passively in the book. */
  post_only?: boolean
  /** RFC3339 timestamp (e.g. `2021-04-01T00:18:45Z`) after which the matching engine should reject this amend if it is still queued due to latency, minimum now()+2s, maximum now()+60s. */
  deadline?: TimestampIso
}

export const Request: Codec<Request> = t.object({
  txid: t.optional(t.string),
  cl_ord_id: t.optional(t.string),
  order_qty: t.optional(t.string),
  display_qty: t.optional(t.string),
  limit_price: t.optional(t.string),
  trigger_price: t.optional(t.string),
  pair: t.optional(t.string),
  post_only: t.optional(t.boolean),
  deadline: t.optional(t.dateTime),
})

/**
 * Modify an existing order's parameters in place, without cancelling and replacing it: Kraken-assigned and client-assigned order identifiers stay the same, queue priority in the order book is preserved where possible, and if the amended quantity drops below the already-filled quantity the remaining quantity is cancelled.
 *
 * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
 */
export class AmendOrder {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  amendOrder(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Modify an existing order's parameters in place, without cancelling and replacing it: Kraken-assigned and client-assigned order identifiers stay the same, queue priority in the order book is preserved where possible, and if the amended quantity drops below the already-filled quantity the remaining quantity is cancelled.
   *
   * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
   *
   * @see https://docs.kraken.com/api-reference/trading/amend-order
   */
  amendOrder(request?: Request, options?: CallOptions): Promise<OrderAmended>
  async amendOrder(request?: Request, options?: CallOptions): Promise<OrderAmended> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/AmendOrder',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: OrderAmended,
      meta: { signed: true },
      ...options,
    })
  }
}
