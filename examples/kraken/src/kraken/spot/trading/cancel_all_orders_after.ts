// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, type TimestampIso, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** Current state of the dead man's switch timer after this call. */
export interface DeadMansSwitchStatus {
  /** RFC3339 timestamp at which the request was received. */
  currentTime?: TimestampIso
  /** RFC3339 timestamp after which all orders will be cancelled, unless the timer is extended or disabled first. Equal to `currentTime` when the timer was disabled (`timeout: 0`). */
  triggerTime?: TimestampIso
}

export const DeadMansSwitchStatus: Codec<DeadMansSwitchStatus> = t.object({
  currentTime: t.optional(t.dateTime),
  triggerTime: t.optional(t.dateTime),
})

/** Parameters for setting, extending, or disabling the dead man's switch. */
export interface Request {
  /** Duration in seconds to set or extend the timer; must be less than 86400. `0` disables the timer. */
  timeout: number
}

export const Request: Codec<Request> = t.object({
  timeout: t.integer,
})

/**
 * A "dead man's switch" that protects against network malfunction, extreme latency, or unexpected matching-engine downtime. Starts a countdown timer (given in seconds) that cancels every one of the client's orders when it expires; each call resets the countdown. Pass a `timeout` of `0` to disable the mechanism. Recommended usage: call every 15-30 seconds with a 60 second timeout, so a brief disconnection doesn't cancel resting orders but a real network breakdown does. Also recommended: disable the timer before scheduled matching-engine maintenance, since a live timer cancels every order when the engine comes back up from any downtime, planned or not.
 *
 * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
 */
export class CancelAllOrdersAfter {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  cancelAllOrdersAfter(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * A "dead man's switch" that protects against network malfunction, extreme latency, or unexpected matching-engine downtime. Starts a countdown timer (given in seconds) that cancels every one of the client's orders when it expires; each call resets the countdown. Pass a `timeout` of `0` to disable the mechanism. Recommended usage: call every 15-30 seconds with a 60 second timeout, so a brief disconnection doesn't cancel resting orders but a real network breakdown does. Also recommended: disable the timer before scheduled matching-engine maintenance, since a live timer cancels every order when the engine comes back up from any downtime, planned or not.
   *
   * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
   *
   * @see https://docs.kraken.com/api-reference/trading/cancel-all-orders-after-x
   */
  cancelAllOrdersAfter(request: Request, options?: CallOptions): Promise<DeadMansSwitchStatus>
  async cancelAllOrdersAfter(request: Request, options?: CallOptions): Promise<DeadMansSwitchStatus> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/CancelAllOrdersAfter',
      request,
      requestCodec: Request,
      responseCodec: DeadMansSwitchStatus,
      meta: { signed: true },
      ...options,
    })
  }
}
