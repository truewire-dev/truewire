// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** Result of a cancel request. */
export interface OrderCancelled {
  /** Number of orders cancelled. */
  count?: number
  /** If true, orders are pending cancellation. */
  pending?: boolean
}

export const OrderCancelled: Codec<OrderCancelled> = t.object({
  count: t.optional(t.integer),
  pending: t.optional(t.boolean),
})

/** Parameters identifying the order(s) to cancel. */
export interface Request {
  /** Kraken order identifier (txid), or a user reference (userref) to cancel every order sharing it. */
  txid?: string | number
  /** An alphanumeric client order identifier which uniquely identifies an open order for this client. */
  cl_ord_id?: string
}

export const Request: Codec<Request> = t.object({
  txid: t.optional(t.union(t.string, t.integer)),
  cl_ord_id: t.optional(t.string),
})

/**
 * Cancel a particular open order (or set of open orders sharing a `userref`) by `txid`, `userref` or `cl_ord_id`.
 *
 * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
 */
export class CancelOrder {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  cancelOrder(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Cancel a particular open order (or set of open orders sharing a `userref`) by `txid`, `userref` or `cl_ord_id`.
   *
   * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
   *
   * @see https://docs.kraken.com/api-reference/trading/cancel-order
   */
  cancelOrder(request?: Request, options?: CallOptions): Promise<OrderCancelled>
  async cancelOrder(request?: Request, options?: CallOptions): Promise<OrderCancelled> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/CancelOrder',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: OrderCancelled,
      meta: { signed: true },
      ...options,
    })
  }
}
