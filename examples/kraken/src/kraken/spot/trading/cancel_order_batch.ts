// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** Result of a cancel request. */
export interface OrderCancelled {
  /** Number of orders cancelled. */
  count?: number
}

export const OrderCancelled: Codec<OrderCancelled> = t.object({
  count: t.optional(t.integer),
})

/** Parameters identifying the orders to cancel, up to 50 unique identifiers total. */
export interface Request {
  /** Open order transaction IDs or user references to cancel, up to 50 total unique IDs/references across `orders` and `cl_ord_ids`. */
  orders?: (string | number)[]
  /** Client order identifiers to cancel, up to 50 total unique IDs/references across `orders` and `cl_ord_ids`. */
  cl_ord_ids?: string[]
}

export const Request: Codec<Request> = t.object({
  orders: t.optional(t.array(t.union(t.string, t.integer))),
  cl_ord_ids: t.optional(t.array(t.string)),
})

/**
 * Cancel multiple open orders by `txid`, `userref` or `cl_ord_id` -- up to 50 unique identifiers/references total across `orders` and `cl_ord_ids`.
 *
 * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
 */
export class CancelOrderBatch {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  cancelOrderBatch(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Cancel multiple open orders by `txid`, `userref` or `cl_ord_id` -- up to 50 unique identifiers/references total across `orders` and `cl_ord_ids`.
   *
   * **API Key Permissions Required:** `Orders and trades - Create & modify orders` or `Orders and trades - Cancel & close orders`
   *
   * @see https://docs.kraken.com/api-reference/trading/cancel-order-batch
   */
  cancelOrderBatch(request?: Request, options?: CallOptions): Promise<OrderCancelled>
  async cancelOrderBatch(request?: Request, options?: CallOptions): Promise<OrderCancelled> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/CancelOrderBatch',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: OrderCancelled,
      meta: { signed: true },
      ...options,
    })
  }
}
