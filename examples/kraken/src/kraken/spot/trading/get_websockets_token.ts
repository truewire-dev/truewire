// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type HttpEndpoint, t } from '@truewire/core'
import type { SpotMeta } from '../../meta.js'

/** A short-lived token authenticating a private WebSocket connection. */
export interface WebSocketsToken {
  /** WebSocket authentication token. */
  token?: string
  /** Seconds after which the token expires (~900), unless already consumed by a private WebSocket subscription. */
  expires?: number
}

export const WebSocketsToken: Codec<WebSocketsToken> = t.object({
  token: t.optional(t.string),
  expires: t.optional(t.integer),
})

/**
 * Request an authentication token for connecting to and authenticating with Kraken's private WebSocket API. The token should be used within 15 minutes of issuance; once a successful connection and private subscription has been made, it does not expire for the life of that connection.
 *
 * **API Key Permissions Required:** `WebSocket interface - On`
 */
export class GetWebsocketsToken {
  constructor(readonly core: HttpEndpoint<SpotMeta>) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  getWebsocketsToken(options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Request an authentication token for connecting to and authenticating with Kraken's private WebSocket API. The token should be used within 15 minutes of issuance; once a successful connection and private subscription has been made, it does not expire for the life of that connection.
   *
   * **API Key Permissions Required:** `WebSocket interface - On`
   *
   * @see https://docs.kraken.com/api-reference/trading/get-websockets-token
   */
  getWebsocketsToken(options?: CallOptions): Promise<WebSocketsToken>
  async getWebsocketsToken(options?: CallOptions): Promise<WebSocketsToken> {
    return this.core.request({
      method: 'POST',
      path: '/0/private/GetWebSocketsToken',
      request: undefined,
      requestCodec: undefined,
      responseCodec: WebSocketsToken,
      meta: { signed: true },
      ...options,
    })
  }
}
