// Generated by truewire — do not edit by hand.
import type { CommandEndpoint, StreamEndpoint } from '@truewire/core'
import { MarketData } from './market_data/index.js'
import { Private } from './private/index.js'

/** The transports `Streams` is built from, one per field the composite's `truewire.toml` declarations name; the hand-written core satisfies it by shape. */
export interface StreamsCore {
  market_client: CommandEndpoint & StreamEndpoint
  private_client: StreamEndpoint
}

/**
 * Kraken's Spot WebSocket v2 API: public real-time market data channels (ticker,
 * book, trade, OHLC, instrument, status) over one connection, and authenticated private
 * account channels (balances, executions) over a second, token-authenticated connection.
 *
 * @see https://docs.kraken.com/exchange/api-reference/spot-websocket
 */
export class Streams {
  /**
   * Public Spot WebSocket v2 channels: ticker, order book, trade, OHLC, instrument
   * reference data, and connection status/heartbeat -- no authentication required.
   */
  readonly marketData: MarketData
  /**
   * Private, token-authenticated Spot WebSocket v2 channels: real-time balance
   * updates and order execution reports.
   */
  readonly private: Private

  constructor(readonly core: StreamsCore) {
    this.marketData = new MarketData(core.market_client)
    this.private = new Private(core.private_client)
  }
}
