// Generated by truewire — do not edit by hand.
import type { CallOptions, CommandEndpoint, StreamEndpoint, Subscription } from '@truewire/core'
import * as book from './book.js'
import * as instrument from './instrument.js'
import * as ohlc from './ohlc.js'
import * as ping from './ping.js'
import * as status from './status.js'
import * as ticker from './ticker.js'
import * as trade from './trade.js'

/**
 * Public Spot WebSocket v2 channels: ticker, order book, trade, OHLC, instrument
 * reference data, and connection status/heartbeat -- no authentication required.
 *
 * @see https://docs.kraken.com/exchange/api-reference/spot-websocket
 */
export class MarketData {
  private readonly book_: book.Book
  private readonly instrument_: instrument.Instrument
  private readonly ohlc_: ohlc.Ohlc
  private readonly ping_: ping.Ping
  private readonly status_: status.Status
  private readonly ticker_: ticker.Ticker
  private readonly trade_: trade.Trade

  constructor(readonly core: CommandEndpoint & StreamEndpoint) {
    this.book_ = new book.Book(core)
    this.instrument_ = new instrument.Instrument(core)
    this.ohlc_ = new ohlc.Ohlc(core)
    this.ping_ = new ping.Ping(core)
    this.status_ = new status.Status(core)
    this.ticker_ = new ticker.Ticker(core)
    this.trade_ = new trade.Trade(core)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  book(parameters: book.Parameters, options: CallOptions & { validate: false }): Subscription<unknown>
  /**
   * Subscribe to the level 2 (L2) order book -- individual price levels with aggregated order quantities at each level -- for one or more currency pairs, including a CRC32 checksum of the top 10 bids and asks for local book verification.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/book
   */
  book(parameters: book.Parameters, options?: CallOptions): Subscription<book.BookMessage>
  book(parameters: book.Parameters, options?: CallOptions): Subscription<book.BookMessage> {
    return this.book_.book(parameters, options)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  instrument(parameters: instrument.Parameters, options: CallOptions & { validate: false }): Subscription<unknown>
  /**
   * Subscribe to reference data for all active assets and tradeable pairs -- symbol identifiers, precisions, and trading parameters and rules.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/instrument
   */
  instrument(parameters?: instrument.Parameters, options?: CallOptions): Subscription<instrument.InstrumentMessage>
  instrument(parameters?: instrument.Parameters, options?: CallOptions): Subscription<instrument.InstrumentMessage> {
    return this.instrument_.instrument(parameters, options)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  ohlc(parameters: ohlc.Parameters, options: CallOptions & { validate: false }): Subscription<unknown>
  /**
   * Subscribe to Open/High/Low/Close (OHLC) candle data at a given interval for one or more currency pairs. Updates are generated on trade events.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/ohlc
   */
  ohlc(parameters: ohlc.Parameters, options?: CallOptions): Subscription<ohlc.OhlcMessage>
  ohlc(parameters: ohlc.Parameters, options?: CallOptions): Subscription<ohlc.OhlcMessage> {
    return this.ohlc_.ohlc(parameters, options)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  ping(options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Application-level ping to verify the WebSocket connection is alive -- distinct from the WebSocket protocol's own ping frames. The server answers with a `pong`. Useful to keep an otherwise-idle connection open (Kraken closes any connection after roughly a minute of inactivity).
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/ping
   */
  ping(options?: CallOptions): Promise<ping.PongReply>
  ping(options?: CallOptions): Promise<ping.PongReply> {
    return this.ping_.ping(options)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  status(options: CallOptions & { validate: false }): Subscription<unknown>
  /**
   * Subscribe to exchange status: automatically pushed once on successful connection and again whenever the trading engine status changes. There is no way to directly request a fresh status update.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/status
   */
  status(options?: CallOptions): Subscription<status.StatusMessage>
  status(options?: CallOptions): Subscription<status.StatusMessage> {
    return this.status_.status(options)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  ticker(parameters: ticker.Parameters, options: CallOptions & { validate: false }): Subscription<unknown>
  /**
   * Subscribe to level 1 market data (top of book best bid/offer, and recent trade data) for one or more currency pairs. Updates are generated on trade events by default, or on best-bid-offer changes when `event_trigger` is `bbo`.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/ticker
   */
  ticker(parameters: ticker.Parameters, options?: CallOptions): Subscription<ticker.TickerMessage>
  ticker(parameters: ticker.Parameters, options?: CallOptions): Subscription<ticker.TickerMessage> {
    return this.ticker_.ticker(parameters, options)
  }

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  trade(parameters: trade.Parameters, options: CallOptions & { validate: false }): Subscription<unknown>
  /**
   * Subscribe to trade events for one or more currency pairs. A push may batch multiple trades, which does not imply they resulted from a single taker order.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/trade
   */
  trade(parameters: trade.Parameters, options?: CallOptions): Subscription<trade.TradeMessage>
  trade(parameters: trade.Parameters, options?: CallOptions): Subscription<trade.TradeMessage> {
    return this.trade_.trade(parameters, options)
  }
}
