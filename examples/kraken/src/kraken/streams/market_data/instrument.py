# Generated by truewire — do not edit by hand.
from truewire_core.util import StreamManager
from typing_extensions import Any, Literal, NotRequired, TypedDict
from kraken.core.endpoint.socket import SocketEndpoint


InstrumentAssetKeywords = TypedDict(
  'InstrumentAssetKeywords', {'class': NotRequired[str]}
)
"""
- `class`: Asset class, e.g. `currency`.
"""


class InstrumentAsset(InstrumentAssetKeywords):
  """Reference data for one asset."""

  id: str
  """Asset identifier."""
  status: NotRequired[
    Literal[
      'depositonly',
      'disabled',
      'enabled',
      'fundingtemporarilydisabled',
      'withdrawalonly',
      'workinprogress',
    ]
  ]
  """Status of the asset."""
  precision: NotRequired[int]
  """Maximum precision for asset ledger, balances."""
  precision_display: NotRequired[int]
  """Recommended display precision."""
  borrowable: NotRequired[bool]
  """Flag if asset is borrowable."""
  collateral_value: NotRequired[float]
  """Valuation as margin collateral, if applicable."""
  margin_rate: NotRequired[float]
  """Interest rate to borrow the asset."""
  multiplier: NotRequired[float]
  """Multiplier of the tokenised asset -- fixed conversion rate of the token."""


class InstrumentPair(TypedDict):
  """Reference data for one tradeable pair."""

  symbol: str
  """The symbol of the currency pair, e.g. `"BTC/USD"`."""
  base: NotRequired[str]
  """Asset identifier of the base currency."""
  quote: NotRequired[str]
  """Asset identifier of the quote currency."""
  status: NotRequired[
    Literal[
      'cancel_only',
      'delisted',
      'limit_only',
      'maintenance',
      'online',
      'post_only',
      'reduce_only',
      'work_in_progress',
    ]
  ]
  """Status of the pair."""
  qty_precision: NotRequired[int]
  """Maximum precision used for order quantities."""
  qty_increment: NotRequired[float]
  """Minimum quantity increment for new orders."""
  qty_min: NotRequired[float]
  """Minimum quantity, in base currency, for new orders."""
  price_precision: NotRequired[int]
  """Maximum precision used for order prices."""
  price_increment: NotRequired[float]
  """Minimum price increment for new orders."""
  cost_precision: NotRequired[int]
  """Maximum precision used for cost prices."""
  cost_min: NotRequired[float]
  """Minimum cost (price * qty) for new orders."""
  has_index: NotRequired[bool]
  """Whether the pair has an index available, e.g. for stop-loss triggers."""
  marginable: NotRequired[bool]
  """Whether the pair can be traded on margin."""
  margin_initial: NotRequired[float]
  """Initial margin requirement, in percent. Present on marginable pairs only."""
  position_limit_long: NotRequired[int]
  """Limit for long positions. Present on marginable pairs only."""
  position_limit_short: NotRequired[int]
  """Limit for short positions. Present on marginable pairs only."""
  ws_display_price_precision: NotRequired[int]
  """Recommended display precision for prices in WebSocket feeds."""
  tick_size: NotRequired[float]
  """Minimum price increment for new orders. Deprecated -- use `price_increment`."""


class Parameters(TypedDict):
  """Parameters for an `instrument` channel subscription. Unlike the other market-data channels, this one takes no `symbol` list -- it always streams the full reference data set."""

  execution_venue: NotRequired[Literal['international', 'bitnomial-exchange']]
  """The execution venue for the instrument channel."""
  include_tokenized_assets: NotRequired[bool]
  """If `true`, include xStocks in the response; otherwise include crypto spot pairs only."""
  snapshot: NotRequired[bool]
  """Request a snapshot after subscribing."""


class InstrumentData(TypedDict):
  """The reference-data set carried by this push."""

  assets: list[InstrumentAsset]
  """Assets included in this push."""
  pairs: list[InstrumentPair]
  """Tradeable pairs included in this push."""


class InstrumentMessage(TypedDict):
  """An `instrument` channel push. Unlike the other market-data channels, `data` is a single object (an `{assets, pairs}` reference-data set), not an array of per-symbol entries."""

  channel: Literal['instrument']
  """The channel this push belongs to."""
  type: Literal['snapshot', 'update']
  """Whether this push is the initial snapshot or an incremental update."""
  data: InstrumentData


class Instrument(SocketEndpoint):
  """Subscribe to reference data for all active assets and tradeable pairs -- symbol identifiers, precisions, and trading parameters and rules."""

  def instrument(
    self,
    execution_venue: Literal['international', 'bitnomial-exchange'] | None = None,
    *,
    include_tokenized_assets: bool | None = None,
    snapshot: bool | None = None,
    validate: bool | None = None,
  ) -> StreamManager[InstrumentMessage, Any, Any]:
    """Subscribe to reference data for all active assets and tradeable pairs -- symbol identifiers, precisions, and trading parameters and rules.

    Args:
      execution_venue: The execution venue for the instrument channel.
      include_tokenized_assets: If `true`, include xStocks in the response; otherwise include crypto spot pairs only.
      snapshot: Request a snapshot after subscribing.
      validate: Override this call's pushed-payload validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/instrument)
    """
    parameters: Parameters = Parameters()
    if execution_venue is not None:
      parameters['execution_venue'] = execution_venue
    if include_tokenized_assets is not None:
      parameters['include_tokenized_assets'] = include_tokenized_assets
    if snapshot is not None:
      parameters['snapshot'] = snapshot
    return self.subscribe(
      'instrument',
      parameters,
      validate=validate,
      request_type=Parameters,
      response_type=InstrumentMessage,
    )
