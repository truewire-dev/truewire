// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type CommandEndpoint, type TimestampIso, t } from '@truewire/core'

/** The reply to a `ping` request. */
export interface PongReply {
  /** Always `pong` -- note this differs from the request's own `method` value of `ping`. */
  method: 'pong'
  /** Client-originated request identifier, echoed back if one was sent. */
  req_id?: number
  /** Indicates if the request was successfully processed by the engine. */
  success?: boolean
  /** Error message, present when `success` is `false`. */
  error?: string
  /** RFC3339 timestamp when the request was received on the wire, just prior to parsing. */
  time_in: TimestampIso
  /** RFC3339 timestamp when the response was sent on the wire, just prior to transmitting. */
  time_out: TimestampIso
}

export const PongReply: Codec<PongReply> = t.object({
  method: t.literal('pong'),
  req_id: t.optional(t.integer),
  success: t.optional(t.boolean),
  error: t.optional(t.string),
  time_in: t.dateTime,
  time_out: t.dateTime,
})

/** Application-level ping to verify the WebSocket connection is alive -- distinct from the WebSocket protocol's own ping frames. The server answers with a `pong`. Useful to keep an otherwise-idle connection open (Kraken closes any connection after roughly a minute of inactivity). */
export class Ping {
  constructor(readonly core: CommandEndpoint) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  ping(options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Application-level ping to verify the WebSocket connection is alive -- distinct from the WebSocket protocol's own ping frames. The server answers with a `pong`. Useful to keep an otherwise-idle connection open (Kraken closes any connection after roughly a minute of inactivity).
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/ping
   */
  ping(options?: CallOptions): Promise<PongReply>
  async ping(options?: CallOptions): Promise<PongReply> {
    return this.core.request({
      path: 'ping',
      request: undefined,
      requestCodec: undefined,
      responseCodec: PongReply,
      meta: {},
      ...options,
    })
  }
}
