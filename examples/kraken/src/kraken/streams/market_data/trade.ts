// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type StreamEndpoint, type Subscription, type TimestampIso, t } from '@truewire/core'

/** Parameters for a `trade` channel subscription. */
export interface Parameters {
  /** Currency pairs to subscribe to, e.g. `["BTC/USD", "MATIC/GBP"]`. */
  symbol: string[]
  /** Request a snapshot of the most recent 50 trades after subscribing. */
  snapshot?: boolean
}

export const Parameters: Codec<Parameters> = t.object({
  symbol: t.array(t.string),
  snapshot: t.optional(t.boolean),
})

/** One matched trade. */
export interface TradeData {
  /** The symbol of the currency pair, e.g. `"BTC/USD"`. */
  symbol: string
  /** The side of the taker order. */
  side: string
  /** Size of the trade. */
  qty: number
  /** Average price of the trade. */
  price: number
  /** The order type of the taker order. */
  ord_type: 'limit' | 'market'
  /** Trade identifier -- a sequence number, unique per book. */
  trade_id: number
  /** RFC3339 timestamp of this trade. */
  timestamp: TimestampIso
}

export const TradeData: Codec<TradeData> = t.object({
  symbol: t.string,
  side: t.string,
  qty: t.number,
  price: t.number,
  ord_type: t.literal('limit', 'market'),
  trade_id: t.integer,
  timestamp: t.dateTime,
})

/** A `trade` channel push. */
export interface TradeMessage {
  /** The channel this push belongs to. */
  channel: 'trade'
  /** Whether this push is the initial snapshot or an incremental update. */
  type: 'snapshot' | 'update'
  /** A list of trade events. */
  data: TradeData[]
}

export const TradeMessage: Codec<TradeMessage> = t.object({
  channel: t.literal('trade'),
  type: t.literal('snapshot', 'update'),
  data: t.array(TradeData),
})

/** Subscribe to trade events for one or more currency pairs. A push may batch multiple trades, which does not imply they resulted from a single taker order. */
export class Trade {
  constructor(readonly core: StreamEndpoint) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  trade(parameters: Parameters, options: CallOptions & { validate: false }): Subscription<unknown>
  /**
   * Subscribe to trade events for one or more currency pairs. A push may batch multiple trades, which does not imply they resulted from a single taker order.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/trade
   */
  trade(parameters: Parameters, options?: CallOptions): Subscription<TradeMessage>
  trade(parameters: Parameters, options?: CallOptions): Subscription<TradeMessage> {
    return this.core.subscribe({
      channel: 'trade',
      parameters,
      parametersCodec: Parameters,
      messageCodec: TradeMessage,
      meta: {},
      ...options,
    })
  }
}
