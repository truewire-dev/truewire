// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type CommandEndpoint, type TimestampIso, t } from '@truewire/core'
import { OrderTriggers, WsConditionalCloseOrder } from '../types/index.js'

/** Result of a successful `add_order` call. */
export interface AddOrderResult {
  /** Unique order identifier generated by Kraken. Absent when `validate=true`, since no order actually reaches the matching engine. */
  order_id?: string
  /** The client identifier specified in the request `params`, if any. */
  cl_ord_id?: string
  /** The client identifier specified in the request `params`, if any. */
  order_userref?: number
  /** Non-fatal warnings about the order, if any. */
  warnings?: string[]
}

export const AddOrderResult: Codec<AddOrderResult> = t.object({
  order_id: t.optional(t.string),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  warnings: t.optional(t.array(t.string)),
})

/** An iceberg order: a limit order that shows only `display_qty` of its quantity in the book while the rest remains hidden. */
export interface WsAddOrderIceberg {
  /** The execution model of the order. */
  order_type: 'iceberg'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  /** Limit price, for order types that support a limit price restriction. */
  limit_price: number
  /** For `iceberg` orders only: quantity to show in the book while the rest of `order_qty` remains hidden. Minimum value is 1/15 of `order_qty`. */
  display_qty: number
}

export const WsAddOrderIceberg: Codec<WsAddOrderIceberg> = t.object({
  order_type: t.literal('iceberg'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  limit_price: t.number,
  display_qty: t.number,
})

/** A limit order: rests in the book at `limit_price` or better. */
export interface WsAddOrderLimit {
  /** The execution model of the order. */
  order_type: 'limit'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  /** Limit price, for order types that support a limit price restriction. */
  limit_price: number
}

export const WsAddOrderLimit: Codec<WsAddOrderLimit> = t.object({
  order_type: t.literal('limit'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  limit_price: t.number,
})

/** A market order: executes immediately at the best available price. */
export interface WsAddOrderMarket {
  /** The execution model of the order. */
  order_type: 'market'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  /** Order volume expressed in the quote currency. For buy market orders without margin funding. */
  cash_order_qty?: number
}

export const WsAddOrderMarket: Codec<WsAddOrderMarket> = t.object({
  order_type: t.literal('market'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  cash_order_qty: t.optional(t.number),
})

/** Settles an open leveraged position at the current market price. */
export interface WsAddOrderSettlePosition {
  /** The execution model of the order. */
  order_type: 'settle-position'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
}

export const WsAddOrderSettlePosition: Codec<WsAddOrderSettlePosition> = t.object({
  order_type: t.literal('settle-position'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
})

/** A stop-loss order: becomes a market order once `triggers.price` is reached in an unfavourable direction. */
export interface WsAddOrderStopLoss {
  /** The execution model of the order. */
  order_type: 'stop-loss'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  triggers: OrderTriggers
}

export const WsAddOrderStopLoss: Codec<WsAddOrderStopLoss> = t.object({
  order_type: t.literal('stop-loss'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  triggers: OrderTriggers,
})

/** A stop-loss-limit order: becomes a limit order at `limit_price` once `triggers.price` is reached in an unfavourable direction. */
export interface WsAddOrderStopLossLimit {
  /** The execution model of the order. */
  order_type: 'stop-loss-limit'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  triggers: OrderTriggers
  /** Limit price, for order types that support a limit price restriction. */
  limit_price: number
}

export const WsAddOrderStopLossLimit: Codec<WsAddOrderStopLossLimit> = t.object({
  order_type: t.literal('stop-loss-limit'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  triggers: OrderTriggers,
  limit_price: t.number,
})

/** A take-profit order: becomes a market order once `triggers.price` is reached in a favourable direction. */
export interface WsAddOrderTakeProfit {
  /** The execution model of the order. */
  order_type: 'take-profit'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  triggers: OrderTriggers
}

export const WsAddOrderTakeProfit: Codec<WsAddOrderTakeProfit> = t.object({
  order_type: t.literal('take-profit'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  triggers: OrderTriggers,
})

/** A take-profit-limit order: becomes a limit order at `limit_price` once `triggers.price` is reached in a favourable direction. */
export interface WsAddOrderTakeProfitLimit {
  /** The execution model of the order. */
  order_type: 'take-profit-limit'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  triggers: OrderTriggers
  /** Limit price, for order types that support a limit price restriction. */
  limit_price: number
}

export const WsAddOrderTakeProfitLimit: Codec<WsAddOrderTakeProfitLimit> = t.object({
  order_type: t.literal('take-profit-limit'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  triggers: OrderTriggers,
  limit_price: t.number,
})

/** A trailing-stop order: triggers as a market order once the price reverts by `triggers.price` from its peak/trough since order entry. */
export interface WsAddOrderTrailingStop {
  /** The execution model of the order. */
  order_type: 'trailing-stop'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  triggers: OrderTriggers
}

export const WsAddOrderTrailingStop: Codec<WsAddOrderTrailingStop> = t.object({
  order_type: t.literal('trailing-stop'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  triggers: OrderTriggers,
})

/** A trailing-stop-limit order: triggers a limit order at `limit_price` (an offset from the trigger price, per `limit_price_type`) once the price reverts by `triggers.price` from its peak/trough since order entry. */
export interface WsAddOrderTrailingStopLimit {
  /** The execution model of the order. */
  order_type: 'trailing-stop-limit'
  /** Side of the order. */
  side: 'buy' | 'sell'
  /** Order quantity in terms of the base asset. */
  order_qty: number
  /** The symbol of the currency pair, e.g. `BTC/USD`. */
  symbol: string
  /** How long the order remains in effect before expiring: `gtc` (Good Till Canceled), `gtd` (Good Till Date -- until `expire_time`), `ioc` (Immediate Or Cancel -- cancels any unfilled remainder on arrival), or `fok` (Fill Or Kill -- fills the full quantity or cancels entirely; `limit` orders only). */
  time_in_force?: 'gtc' | 'gtd' | 'ioc' | 'fok'
  /** Funds the order on margin using the maximum leverage for the pair (max leverage of 5). */
  margin?: boolean
  /** Cancels the order if it would take liquidity on arrival; post-only orders are always posted passively in the book. Only for orders with a limit price. */
  post_only?: boolean
  /** Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets. */
  reduce_only?: boolean
  /** Scheduled start time, RFC3339, precision to seconds. */
  effective_time?: TimestampIso
  /** Expiration time of the order, RFC3339, precision to seconds. GTD orders can expire up to one month in the future. */
  expire_time?: TimestampIso
  /** RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time -- protection against latency on time-sensitive orders. */
  deadline?: TimestampIso
  /** Alphanumeric client order identifier uniquely identifying an open order for this client. Mutually exclusive with `order_userref`. One of: a long UUID (32 hex chars, dashed), a short UUID (32 hex chars, no dashes), or free text up to 18 ASCII characters. */
  cl_ord_id?: string
  /** Optional non-unique numeric identifier associable with any number of orders by the client (e.g. a timestamp). Not enforced unique by Kraken, so it can also group orders by pair/side/strategy for querying or cancelling. Mutually exclusive with `cl_ord_id`. */
  order_userref?: number
  conditional?: WsConditionalCloseOrder
  /** Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders. */
  fee_preference?: 'base' | 'quote'
  /** Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection (which protects market orders from filling at a bad price during slippage) for market orders. */
  no_mpp?: boolean
  /** Self Trade Prevention mode, protecting against inadvertently trading against oneself: `cancel_newest` (cancel the arriving order), `cancel_oldest` (cancel the resting order), or `cancel_both`. */
  stp_type?: 'cancel_newest' | 'cancel_oldest' | 'cancel_both'
  /** If `true`, the order is validated only -- it never reaches the matching engine and no `order_id` is returned. */
  validate?: boolean
  /** Alphanumeric sub-account/trader identifier enabling Self Trade Prevention at a more granular level, for institutional accounts with enhanced STP. Same three accepted formats as `cl_ord_id`. */
  sender_sub_id?: string
  /** Deprecated -- use the `triggers` object. Stop price for trigger order types. */
  stop_price?: number
  /** Deprecated -- use `triggers.reference`. Reference price to trigger the order, for triggered order types only. */
  trigger?: 'index' | 'last'
  triggers: OrderTriggers
  /** Limit price, for order types that support a limit price restriction. */
  limit_price: number
  /** Units for `limit_price`: `static` (a static market price), `pct` (a percentage offset from the reference price), or `quote` (a notional offset in the quote currency). Only available on `trailing-stop-limit` orders, where it is an offset from the trigger price (`0` sets the limit price equal to the trigger price). */
  limit_price_type?: 'static' | 'pct' | 'quote'
}

export const WsAddOrderTrailingStopLimit: Codec<WsAddOrderTrailingStopLimit> = t.object({
  order_type: t.literal('trailing-stop-limit'),
  side: t.literal('buy', 'sell'),
  order_qty: t.number,
  symbol: t.string,
  time_in_force: t.optional(t.literal('gtc', 'gtd', 'ioc', 'fok')),
  margin: t.optional(t.boolean),
  post_only: t.optional(t.boolean),
  reduce_only: t.optional(t.boolean),
  effective_time: t.optional(t.dateTime),
  expire_time: t.optional(t.dateTime),
  deadline: t.optional(t.dateTime),
  cl_ord_id: t.optional(t.string),
  order_userref: t.optional(t.integer),
  conditional: t.optional(WsConditionalCloseOrder),
  fee_preference: t.optional(t.literal('base', 'quote')),
  no_mpp: t.optional(t.boolean),
  stp_type: t.optional(t.literal('cancel_newest', 'cancel_oldest', 'cancel_both')),
  validate: t.optional(t.boolean),
  sender_sub_id: t.optional(t.string),
  stop_price: t.optional(t.number),
  trigger: t.optional(t.literal('index', 'last')),
  triggers: OrderTriggers,
  limit_price: t.number,
  limit_price_type: t.optional(t.literal('static', 'pct', 'quote')),
})

export type Request = WsAddOrderMarket | WsAddOrderLimit | WsAddOrderIceberg | WsAddOrderStopLoss | WsAddOrderStopLossLimit | WsAddOrderTakeProfit | WsAddOrderTakeProfitLimit | WsAddOrderTrailingStop | WsAddOrderTrailingStopLimit | WsAddOrderSettlePosition

export const Request: Codec<Request> = t.union(WsAddOrderMarket, WsAddOrderLimit, WsAddOrderIceberg, WsAddOrderStopLoss, WsAddOrderStopLossLimit, WsAddOrderTakeProfit, WsAddOrderTakeProfitLimit, WsAddOrderTrailingStop, WsAddOrderTrailingStopLimit, WsAddOrderSettlePosition)

/** Sends a single, new order into the exchange. A range of order types, Time-In-Force (TIF) settings and order flags can be specified. For triggered order types (`stop-loss`, `take-profit`, `trailing-stop`), `triggers` carries the price-tracking and trigger-threshold parameters. For One-Triggers-Other (OTO) orders, `conditional` carries the parameters for a secondary close order attached to the primary order. */
export class AddOrder {
  constructor(readonly core: CommandEndpoint) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  addOrder(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Sends a single, new order into the exchange. A range of order types, Time-In-Force (TIF) settings and order flags can be specified. For triggered order types (`stop-loss`, `take-profit`, `trailing-stop`), `triggers` carries the price-tracking and trigger-threshold parameters. For One-Triggers-Other (OTO) orders, `conditional` carries the parameters for a secondary close order attached to the primary order.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/add_order
   */
  addOrder(request: Request, options?: CallOptions): Promise<AddOrderResult>
  async addOrder(request: Request, options?: CallOptions): Promise<AddOrderResult> {
    return this.core.request({
      path: 'add_order',
      request,
      requestCodec: Request,
      responseCodec: AddOrderResult,
      meta: {},
      ...options,
    })
  }
}
