// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type CommandEndpoint, t } from '@truewire/core'

/** Result of a successful `cancel_all` call. */
export interface CancelAllResult {
  /** Number of orders cancelled. */
  count?: number
  /** Non-fatal warnings about the cancellation, if any. */
  warnings?: string[]
}

export const CancelAllResult: Codec<CancelAllResult> = t.object({
  count: t.optional(t.integer),
  warnings: t.optional(t.array(t.string)),
})

/** No caller-supplied parameters beyond the transport-injected auth token. */
export interface Request {}

export const Request: Codec<Request> = t.object({})

/** Cancels all open orders on the account, including untriggered orders and orders resting in the book. The details of each individual cancelled order are also streamed on the `executions` channel. */
export class CancelAll {
  constructor(readonly core: CommandEndpoint) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  cancelAll(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Cancels all open orders on the account, including untriggered orders and orders resting in the book. The details of each individual cancelled order are also streamed on the `executions` channel.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/cancel_all
   */
  cancelAll(request?: Request, options?: CallOptions): Promise<CancelAllResult>
  async cancelAll(request?: Request, options?: CallOptions): Promise<CancelAllResult> {
    return this.core.request({
      path: 'cancel_all',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: CancelAllResult,
      meta: {},
      ...options,
    })
  }
}
