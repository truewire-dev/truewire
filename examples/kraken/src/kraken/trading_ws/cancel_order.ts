// Generated by truewire — do not edit by hand.
import { type CallOptions, type Codec, type CommandEndpoint, t } from '@truewire/core'

/** Result describing one cancelled order. */
export interface CancelOrderResult {
  /** Kraken identifier of the cancelled order. */
  order_id?: string
  /** Client identifier of the cancelled order, if any. */
  cl_ord_id?: string
  /** Non-fatal warnings about the cancellation, if any. */
  warnings?: string[]
}

export const CancelOrderResult: Codec<CancelOrderResult> = t.object({
  order_id: t.optional(t.string),
  cl_ord_id: t.optional(t.string),
  warnings: t.optional(t.array(t.string)),
})

/** Parameters identifying one or more open orders to cancel. Exactly one of `order_id`, `cl_ord_id`, `order_userref` should be supplied -- they cannot be combined in the same request. */
export interface Request {
  /** Kraken `order_id` identifiers to cancel. Cannot be combined with `cl_ord_id` or `order_userref` in the same request. */
  order_id?: string[]
  /** Client `cl_ord_id` identifiers to cancel. Cannot be combined with `order_id` or `order_userref` in the same request. */
  cl_ord_id?: string[]
  /** Client `order_userref` identifiers to cancel. Cannot be combined with `order_id` or `cl_ord_id` in the same request. */
  order_userref?: number[]
}

export const Request: Codec<Request> = t.object({
  order_id: t.optional(t.array(t.string)),
  cl_ord_id: t.optional(t.array(t.string)),
  order_userref: t.optional(t.array(t.integer)),
})

/** Cancels one or more open orders in a single request, identified by a range of client or Kraken identifiers. The details of each cancelled order are also streamed on the `executions` channel. */
export class CancelOrder {
  constructor(readonly core: CommandEndpoint) {}

  /** With `validate: false`: the parsed body as it came, typed `unknown`. */
  cancelOrder(request: Request, options: CallOptions & { validate: false }): Promise<unknown>
  /**
   * Cancels one or more open orders in a single request, identified by a range of client or Kraken identifiers. The details of each cancelled order are also streamed on the `executions` channel.
   *
   * @see https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/cancel_order
   */
  cancelOrder(request?: Request, options?: CallOptions): Promise<CancelOrderResult>
  async cancelOrder(request?: Request, options?: CallOptions): Promise<CancelOrderResult> {
    return this.core.request({
      path: 'cancel_order',
      request: request ?? {},
      requestCodec: Request,
      responseCodec: CancelOrderResult,
      meta: {},
      ...options,
    })
  }
}
