# Generated by truewire — do not edit by hand.
from typing_extensions import Literal, NotRequired, TypedDict
from kraken.core import TimestampIso
from kraken.core.endpoint.socket import SocketEndpoint


class EditOrderResult(TypedDict):
  """Result of a successful `edit_order` call."""

  order_id: NotRequired[str]
  """Unique ID of the new (edited) order."""
  original_order_id: NotRequired[str]
  """ID of the original order that was edited."""
  warnings: NotRequired[list[str]]
  """Non-fatal warnings about the edit, if any."""


class EditOrderTriggers(TypedDict):
  """Trigger price conditions for triggered order types."""

  reference: NotRequired[Literal['index', 'last']]
  """Reference price to track for triggering orders: `index` (the broader-market index price for this pair -- falls back to `last` during index-feed connectivity issues) or `last` (the last traded price in the Kraken order book for this pair)."""
  price: NotRequired[float]
  """Amount for the trigger price -- static or relative, per `price_type`. E.g. 29000.5 with `price_type=static` triggers at that price; 5 with `pct` triggers when price rises 5%; -150 with `quote` triggers when price drops 150 quote-currency units. For `trailing-stop`/`trailing-stop-limit`, represents the reversion from the peak price and is always positive."""
  price_type: NotRequired[Literal['static', 'pct', 'quote']]
  """Units for `price`: `static` (a static market price), `pct` (a percentage offset from the reference price), or `quote` (a notional offset in the quote currency)."""


class Request(TypedDict):
  """Parameters for the legacy cancel-replace edit: the original order is cancelled and a new order created with the adjusted parameters, returning a new `order_id`. Superseded by `amend_order`, which resolves this endpoint's caveats (see notes) and performs better."""

  order_id: str
  """Kraken identifier for the order to edit, e.g. `OFGKYQ-FHPCQ-HUQFEK`."""
  symbol: str
  """Original symbol identifier for the pair, e.g. `BTC/USD`. The symbol cannot be amended."""
  order_qty: NotRequired[float]
  """Order quantity in terms of the base asset."""
  display_qty: NotRequired[float]
  """For iceberg orders only: quantity to show in the book while the rest of `order_qty` remains hidden. Minimum value is 1/15 of `order_qty`."""
  limit_price: NotRequired[float]
  """Limit price, for order types that support a limit price restriction."""
  fee_preference: NotRequired[Literal['base', 'quote']]
  """Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders."""
  no_mpp: NotRequired[bool]
  """Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection for market orders."""
  order_userref: NotRequired[int]
  """User-defined reference to place on the edited order. Does not identify the order to be edited -- use `order_id` for that."""
  post_only: NotRequired[bool]
  """Cancels the order if it would take liquidity on arrival. For orders with a limit price only."""
  reduce_only: NotRequired[bool]
  """Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets."""
  triggers: NotRequired[EditOrderTriggers]
  validate: NotRequired[bool]
  """If `true`, the edit is validated only -- it never reaches the matching engine."""
  deadline: NotRequired[TimestampIso]
  """RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time."""
  price: NotRequired[float]
  """Deprecated -- use `limit_price`."""
  trigger: NotRequired[str]
  """Deprecated -- use `triggers.reference`."""
  stop_price: NotRequired[float]
  """Deprecated -- use `triggers.price`."""


class EditOrder(SocketEndpoint):
  """Legacy cancel-replace edit of a live order's parameters: the original order is cancelled and a new order is created with the adjusted parameters, returning a new `order_id`. Superseded by `amend_order`, which resolves this endpoint's caveats and performs better: triggered stop-loss/take-profit orders are not supported; orders with attached conditional close terms are not supported; an edit whose executed volume exceeds the newly supplied volume is rejected; `cl_ord_id` is not supported; existing executions stay associated with the original order rather than copying to the edited one; and queue position is not maintained."""

  async def edit_order(
    self,
    *,
    order_id: str,
    symbol: str,
    order_qty: float | None = None,
    display_qty: float | None = None,
    limit_price: float | None = None,
    fee_preference: Literal['base', 'quote'] | None = None,
    no_mpp: bool | None = None,
    order_userref: int | None = None,
    post_only: bool | None = None,
    reduce_only: bool | None = None,
    triggers: EditOrderTriggers | None = None,
    validate_: bool | None = None,
    deadline: TimestampIso | None = None,
    price: float | None = None,
    trigger: str | None = None,
    stop_price: float | None = None,
    validate: bool | None = None,
  ) -> EditOrderResult:
    """Legacy cancel-replace edit of a live order's parameters: the original order is cancelled and a new order is created with the adjusted parameters, returning a new `order_id`. Superseded by `amend_order`, which resolves this endpoint's caveats and performs better: triggered stop-loss/take-profit orders are not supported; orders with attached conditional close terms are not supported; an edit whose executed volume exceeds the newly supplied volume is rejected; `cl_ord_id` is not supported; existing executions stay associated with the original order rather than copying to the edited one; and queue position is not maintained.

    Args:
      order_id: Kraken identifier for the order to edit, e.g. `OFGKYQ-FHPCQ-HUQFEK`.
      symbol: Original symbol identifier for the pair, e.g. `BTC/USD`. The symbol cannot be amended.
      order_qty: Order quantity in terms of the base asset.
      display_qty: For iceberg orders only: quantity to show in the book while the rest of `order_qty` remains hidden. Minimum value is 1/15 of `order_qty`.
      limit_price: Limit price, for order types that support a limit price restriction.
      fee_preference: Fee preference, base or quote currency. `quote` is the default for buy orders, `base` is the default for sell orders.
      no_mpp: Deprecated -- if supplied, accepted but ignored. Was: disable Market Price Protection for market orders.
      order_userref: User-defined reference to place on the edited order. Does not identify the order to be edited -- use `order_id` for that.
      post_only: Cancels the order if it would take liquidity on arrival. For orders with a limit price only.
      reduce_only: Reduces an existing margin position without opening an opposite position worth more than the current value of the leveraged assets.
      triggers: Trigger price conditions for triggered order types.
      validate_: If `true`, the edit is validated only -- it never reaches the matching engine.
      deadline: RFC3339 timestamp, millisecond precision. Valid offsets from the current time range from 500ms to 60s (default 5s). The engine will not match this order after this time.
      price: Deprecated -- use `limit_price`.
      trigger: Deprecated -- use `triggers.reference`.
      stop_price: Deprecated -- use `triggers.price`.
      validate: Override this call's response validation; falls back to the client-level default when omitted.

    References:
      - [Official docs](https://docs.kraken.com/exchange/api-reference/spot-websocket-v2/edit_order)
    """
    request: Request = Request(order_id=order_id, symbol=symbol)
    if order_qty is not None:
      request['order_qty'] = order_qty
    if display_qty is not None:
      request['display_qty'] = display_qty
    if limit_price is not None:
      request['limit_price'] = limit_price
    if fee_preference is not None:
      request['fee_preference'] = fee_preference
    if no_mpp is not None:
      request['no_mpp'] = no_mpp
    if order_userref is not None:
      request['order_userref'] = order_userref
    if post_only is not None:
      request['post_only'] = post_only
    if reduce_only is not None:
      request['reduce_only'] = reduce_only
    if triggers is not None:
      request['triggers'] = triggers
    if validate_ is not None:
      request['validate'] = validate_
    if deadline is not None:
      request['deadline'] = deadline
    if price is not None:
      request['price'] = price
    if trigger is not None:
      request['trigger'] = trigger
    if stop_price is not None:
      request['stop_price'] = stop_price
    return await self.request(
      request,
      path='edit_order',
      validate=validate,
      request_type=Request,
      response_type=EditOrderResult,
    )
