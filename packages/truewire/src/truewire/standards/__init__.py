"""
Checks against `docs/production_standards.md` that don't belong to any existing
`truewire` subcommand's own domain -- see `Finding` for why each one lives here rather
than folded into `spec test` or `docs check`.
"""
