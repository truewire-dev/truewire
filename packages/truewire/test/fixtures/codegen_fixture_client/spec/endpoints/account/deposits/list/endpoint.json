{
  "meta": {"signed": true},
  "spec": {
    "kind": "rpc", "transports": ["http"], "path": "/v1/account/deposits", "method": "GET",
    "request": {"title": "DepositsRequest", "type": "object", "properties": {
      "asset": {"type": "string", "description": "Asset symbol."}
    }, "required": ["asset"]},
    "response": {"title": "DepositList", "type": "object", "properties": {
      "deposits": {"type": "array", "items": {"type": "string"}, "description": "Deposit ids."}
    }}
  }
}
