{"description": "An aggregate grouping (design §3) whose two children -- `get/` and `nested/` -- are each their own bare leaf, composed by multiple inheritance. Used to be a directory both a leaf endpoint and a router grouping itself, before rule 16/S30 refused that shape and this directory's own endpoint.json was moved into `get/`, matching bit2me's own real fix.", "upstream": "https://example.com/docs/mixed-dir", "core": "default"}
