{
  "meta": {},
  "spec": {
    "kind": "rpc", "transports": ["http"], "path": "/v1/token/nfts", "method": "GET",
    "request": {"title": "TokenNftsRequest", "type": "object", "properties": {
      "address": {"type": "string", "description": "Wallet address to query."}
    }, "required": ["address"]},
    "response": {"title": "TokenNfts", "type": "object", "properties": {
      "nfts": {"type": "array", "items": {"type": "string"}, "description": "Owned NFT ids."}
    }}
  }
}
