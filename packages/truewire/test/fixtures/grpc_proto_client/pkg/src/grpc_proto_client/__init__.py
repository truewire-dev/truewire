"""Synthetic client package used only to test `Generator.grpc_endpoint`'s live proto
introspection, shaped like a real betterproto2-generated package (dYdX's own
`typed_dydx.protos` tree) without depending on `betterproto2`/`grpclib` at all."""
