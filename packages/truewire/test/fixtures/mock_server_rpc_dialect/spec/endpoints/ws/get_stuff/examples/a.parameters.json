{
  "description": "Call recorded with nonce 'nonce-a' -- redacted, so a live call's own different nonce still matches once stripped.",
  "parameters": {},
  "payload": {
    "method": "acme_getStuff",
    "params": {"symbol": "ACME", "nonce": "nonce-a"}
  }
}
